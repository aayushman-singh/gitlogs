# pnpm requires native build scripts to be explicitly allowlisted.
# esbuild (vite's bundler) needs its postinstall to fetch the native binary,
# otherwise `vite build` fails on a clean clone (especially on Windows).
allowBuilds:
  esbuild: true
