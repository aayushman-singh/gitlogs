# Server Configuration
PORT=3000
NODE_ENV=development

# GitHub Webhook
# Generate a random secret: openssl rand -hex 20
WEBHOOK_SECRET=your_webhook_secret_here

# Twitter API Credentials
# Get from: https://developer.twitter.com/en/portal/dashboard
# Note: Twitter is now X, but the API endpoints remain the same
TWITTER_API_KEY=your_api_key
TWITTER_API_SECRET=your_api_secret
TWITTER_ACCESS_TOKEN=your_access_token
TWITTER_ACCESS_SECRET=your_access_secret

# Optional: Database path for threading support
DATABASE_PATH=./tweets.db

# Optional: Repository filter (comma-separated)
# Leave empty to accept all repos
ALLOWED_REPOS=username/repo1,username/repo2
