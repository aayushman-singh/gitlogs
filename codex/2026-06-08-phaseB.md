Reading additional input from stdin...
OpenAI Codex v0.135.0
--------
workdir: C:\Repo\gitlogs
model: gpt-5.5
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR]
reasoning effort: xhigh
reasoning summaries: none
session id: 019ea7c0-5a37-7fb1-a483-b5585a7b2015
--------
user
review this diff as a senior engineer with no patience for excuses. Find architectural problems, security holes, untested edge cases, naming smell, dead code. Be brutal. No praise.

<stdin>
diff --git a/.github/workflows/ci.yml b/.github/workflows/ci.yml
new file mode 100644
index 0000000..f579f68
--- /dev/null
+++ b/.github/workflows/ci.yml
@@ -0,0 +1,63 @@
+name: CI
+
+on:
+  pull_request:
+  push:
+    branches:
+      - main
+
+concurrency:
+  group: ci-${{ github.ref }}
+  cancel-in-progress: true
+
+jobs:
+  backend:
+    name: Backend (syntax + tests)
+    runs-on: ubuntu-latest
+    steps:
+      - uses: actions/checkout@v4
+
+      - uses: pnpm/action-setup@v4
+        with:
+          version: 9
+
+      - uses: actions/setup-node@v4
+        with:
+          node-version: 20
+          cache: pnpm
+
+      - name: Install (frozen lockfile)
+        run: pnpm install --frozen-lockfile
+
+      # No ESLint is configured yet; use node --check as a lightweight syntax
+      # gate so a broken file fails CI. (Full ESLint setup is a tracked follow-up.)
+      - name: Syntax check (node --check)
+        run: |
+          find src scripts -name '*.js' -print0 | xargs -0 -n1 node --check
+
+      - name: Test (Vitest)
+        run: pnpm test
+
+  frontend:
+    name: Frontend (build)
+    runs-on: ubuntu-latest
+    steps:
+      - uses: actions/checkout@v4
+
+      - uses: pnpm/action-setup@v4
+        with:
+          version: 9
+
+      - uses: actions/setup-node@v4
+        with:
+          node-version: 20
+          cache: pnpm
+          cache-dependency-path: frontend/pnpm-lock.yaml
+
+      - name: Install (frozen lockfile)
+        run: pnpm --dir frontend install --frozen-lockfile
+
+      - name: Build SPA
+        env:
+          VITE_API_BASE: https://api-gitlogs.aayushman.dev
+        run: pnpm --dir frontend run build
diff --git a/.github/workflows/deploy-ec2.yml b/.github/workflows/deploy-ec2.yml
index 95315de..b9a39c2 100644
--- a/.github/workflows/deploy-ec2.yml
+++ b/.github/workflows/deploy-ec2.yml
@@ -18,8 +18,15 @@ jobs:
           key: ${{ secrets.EC2_SSH_KEY }}
           port: ${{ secrets.EC2_PORT || 22 }}
           script: |
-            set -e
+            set -euo pipefail
             cd ~/gitlogs
-            git pull
-            pnpm i
+            git pull --ff-only
+            # Reproducible installs from the committed lockfiles.
+            pnpm install --frozen-lockfile
+            pnpm --dir frontend install --frozen-lockfile
+            # Build the SPA BEFORE restarting — the backend serves frontend/dist
+            # (src/server.js). Without this the served frontend is stale/missing.
+            VITE_API_BASE="${VITE_API_BASE:-https://api-gitlogs.aayushman.dev}" \
+              pnpm --dir frontend run build
             sudo systemctl restart gitlogs
+            sudo systemctl --no-pager status gitlogs | head -n 5
diff --git a/docs/ARCHITECTURE.md b/docs/ARCHITECTURE.md
new file mode 100644
index 0000000..7aa9baa
--- /dev/null
+++ b/docs/ARCHITECTURE.md
@@ -0,0 +1,285 @@
+# GitLogs — Architecture
+
+## System overview
+
+GitLogs turns a `git push` into a published changelog tweet. A developer signs in with GitHub, picks a repository, and GitLogs installs a `push` webhook on it. From then on, every push fires `POST /webhook/github`; the backend verifies the GitHub HMAC signature, resolves which user owns the repo, and for each non-merge commit runs a two-stage AI pipeline — Stage 1 fetches the real commit diff from the GitHub REST API and asks Gemini to summarize it factually; Stage 2 feeds that grounded summary into the user's prompt template (AI-expanded only if the template contains `{{AI_TEXT}}`) to produce tweet text. The text is posted to the developer's own X account via OAuth 2.0 PKCE, optionally quoting a pinned "OG" tweet, and the resulting tweet ID is persisted. All Gemini calls flow through a rate-limited, retrying, restart-survivable queue backed by SQLite (sql.js). The whole thing is a single Node/Express process serving a React/Vite SPA from `frontend/dist`.
+
+---
+
+## Component map
+
+### Backend modules (`src/`)
+
+| Node | Responsibility |
+| --- | --- |
+| `server.js` | Express wiring: static SPA hosting, CORS, cookie parsing, **raw-body capture for the webhook** (`server.js:29-36`), GitHub + X OAuth routes, `/api/me/*` user routes, admin `/api/*` routes (API-key gated), the GitHub API proxy, graceful shutdown. Mounts the webhook at `server.js:832`. |
+| `webhookHandler.js` | The core pipeline. `handleWebhook` (`:269`) verifies the signature, parses JSON or `application/x-www-form-urlencoded` payloads, filters non-`push` events and merge commits, resolves repo→user→X-auth, and drives `processCommit` (`:132`) per commit. |
+| `diffAnalyzer.js` | `fetchCommitDiff` (`:27`) calls the GitHub REST API `GET /repos/{owner}/{repo}/commits/{sha}`, truncates the combined patch to `MAX_DIFF_SIZE` (4000 chars) over at most `MAX_FILES_TO_ANALYZE` (10) files, and builds the Stage-1 analysis prompt. Also `shouldSkipDiffAnalysis` (assets-only / >50 files) and `buildFileBasedSummary` (file-name heuristic when the API gives nothing). |
+| `geminiClient.js` | Two-stage AI. `analyzeDiff` (Stage 1, `:115`) summarizes the diff. `generateChangelog` (Stage 2, `:317`) builds a serializable task and **enqueues it on the queue service**; the task runs the template through `templateEngine`, calls Gemini only if `{{AI_TEXT}}` is present, and injects the Stage-1 summary as anti-hallucination grounding. Initializes the model and registers task factories on load. |
+| `queueService.js` | Singleton rate-limited work queue. Caps to `maxRequestsPerMinute`, retries up to `maxRetries` with jittered exponential backoff (longer floor for rate-limit errors), persists every item to `queue_items`, and restores pending/processing items on startup. Driven by a `setInterval` every `processingIntervalMs`. |
+| `twitterClient.js` | `postTweet` (`:177`) posts via the X API v2 `POST /2/tweets` (twitter-api-sdk). Per-user lazy clients keyed by `userId`, automatic token refresh on 401 with one retry, and quote-tweet support for OG posts. |
+| `oauthHandler.js` | X OAuth 2.0 + PKCE: `generateAuthUrl`, `exchangeCodeForTokens`, `_refreshToken`, `getAccessToken`. Talks directly to `https://api.x.com/2/oauth2/token`. |
+| `githubAuth.js` | GitHub OAuth: auth URL, code exchange, **token refresh with a 10-minute pre-expiry buffer** (`getValidAccessToken`, `:127`), repo listing, and webhook create/delete (`createWebhook`/`deleteWebhook`). |
+| `pkceHelper.js` | RFC 7636 PKCE primitives — `code_verifier`, `S256` `code_challenge`. |
+| `database.js` | sql.js (WASM SQLite) persisted to `tweets.db`. Schema creation, additive `tweets` column migration via `PRAGMA table_info`, and all data access. Auto-saves every 30s and on shutdown. |
+| `config/config.js` | Single env-config surface (server, github, twitter, gemini, queue, multiUser). |
+| `commitFormatter.js`, `templateEngine.js`, `repoIndexer.js` | Supporting: commit→tweet text formatting and Twitter-length math; prompt-template resolution and `{{AI_TEXT}}` handling; repo-context generation from webhook payload (no local clone). |
+
+### Frontend
+React 18 + Vite 5 SPA under `frontend/`, built to `frontend/dist` and served by the backend via `express.static` in production (`server.js:18-21`, SPA fallback at `:1094`). Talks to the backend through `VITE_API_BASE`; auth state rides on the `httpOnly` `github_user_id` cookie.
+
+### External services
+- **GitHub API** (`api.github.com`) — OAuth, repo listing, webhook management, and per-commit diff fetch.
+- **Gemini API** (`@google/generative-ai`, default model `gemini-2.0-flash`) — Stage-1 diff analysis and Stage-2 changelog text.
+- **X API v2** (`api.x.com` / `twitter.com`) — OAuth 2.0 PKCE and tweet creation.
+
+### Store
+- **SQLite via sql.js**, a single file (`DATABASE_PATH`, default `./tweets.db`). In-memory WASM DB exported to disk on a 30s timer and at shutdown.
+
+---
+
+## Webhook flow (sequence)
+
+```mermaid
+sequenceDiagram
+    autonumber
+    participant GH as GitHub
+    participant API as Express /webhook/github
+    participant WH as webhookHandler
+    participant DB as SQLite (sql.js)
+    participant DA as diffAnalyzer
+    participant GHAPI as GitHub REST API
+    participant GEM as geminiClient
+    participant Q as queueService
+    participant GAPI as Gemini API
+    participant TW as twitterClient
+    participant X as X API v2
+
+    GH->>API: POST push event (raw body + x-hub-signature-256)
+    API->>WH: handleWebhook(req)
+    WH->>WH: parse JSON / form payload, extract repository.full_name
+    WH->>DB: getRepoWebhookSecret(repo) (per-repo secret)
+    WH->>WH: HMAC-SHA256(rawBody) compared via timingSafeEqual
+
+    alt signature invalid
+        WH-->>GH: 401 Invalid signature
+    else no secret configured
+        Note over WH: fails OPEN — verification skipped (known weakness)
+        WH->>WH: continue
+    else signature valid
+        WH->>WH: continue
+    end
+
+    alt event != "push"
+        WH-->>GH: 200 Event ignored
+    else push event
+        WH->>DB: isRepoEnabled(repo) / ALLOWED_REPOS
+        alt repo not allowed
+            WH-->>GH: 200 Repository not allowed
+        else repo allowed
+            WH->>DB: getUserByRepo(repo) → userId, xOAuthUserId, githubUserId
+            WH->>DB: getOrGenerateRepoContext(repo) (24h cache)
+            loop each non-merge commit
+                WH->>DA: fetchCommitDiff(repo, sha, githubUserId)
+                DA->>GHAPI: GET /repos/{owner}/{repo}/commits/{sha}
+                GHAPI-->>DA: files + patches + stats
+                DA-->>WH: { diff, files, stats }
+                WH->>GEM: analyzeDiff(diff) — Stage 1
+                GEM->>GAPI: generateContent(analysis prompt)
+                GAPI-->>GEM: factual diff summary
+                GEM-->>WH: diffSummary
+                WH->>GEM: generateChangelog(commit, { diffSummary }) — Stage 2
+                GEM->>Q: enqueue(changelog task, priority NORMAL)
+                Q->>DB: saveQueueItem(pending)
+                loop queue tick (rate-limited, ≤ QUEUE_MAX_RPM)
+                    Q->>GAPI: generateContent(template + diffSummary)
+                    alt success
+                        GAPI-->>Q: changelog text
+                        Q->>DB: deleteQueueItem(id)
+                    else failure & retries left
+                        Note over Q: backoff = base * 2^n (+jitter), re-queue
+                        Q->>DB: incrementQueueItemRetry(id)
+                    else retries exhausted
+                        Q->>DB: updateQueueItemStatus(failed)
+                    end
+                end
+                Q-->>GEM: changelog text
+                GEM-->>WH: changelogText
+                WH->>WH: formatTweetText(changelog, commit, repo)
+                WH->>DB: getOgPost(repo) (optional quote target)
+                WH->>TW: postTweet(text, quoteTweetId, xOAuthUserId)
+                TW->>X: POST /2/tweets
+                alt 401 expired
+                    TW->>X: refresh token, retry once
+                end
+                X-->>TW: tweet id
+                TW-->>WH: tweetId
+                WH->>DB: saveTweetId(repo, sha, tweetId)
+            end
+            WH-->>GH: 200 { processed, total, userId }
+        end
+    end
+```
+
+> Note: `processCommit` swallows per-commit errors and reports `{ success: false }` so one bad commit doesn't abort the batch; the top-level handler also returns `200` even on unexpected errors (`webhookHandler.js:403-406`) to avoid GitHub treating the delivery as failed and retrying.
+
+---
+
+## OAuth flows (high level)
+
+```mermaid
+flowchart TD
+    subgraph GitHubLogin["GitHub login (identity + webhook control)"]
+        A1["User clicks Sign in with GitHub"] --> A2["GET /auth/github<br/>generate state, store in memory"]
+        A2 --> A3["Redirect to github.com/login/oauth/authorize<br/>scope: read:user repo"]
+        A3 --> A4["GitHub callback → GET /auth/github/callback?code,state"]
+        A4 --> A5{"state known?"}
+        A5 -- no --> A6["Redirect /dashboard?error=invalid_state"]
+        A5 -- yes --> A7["exchangeCodeForTokens(code)<br/>access + optional refresh"]
+        A7 --> A8["storeGithubToken + upsertUser<br/>set httpOnly github_user_id cookie"]
+        A8 --> A9["Redirect /dashboard?auth=success"]
+        A9 --> A10["Enable repo → createWebhook(push)<br/>secret = WEBHOOK_SECRET"]
+    end
+
+    subgraph XConnect["X connect (posting) — OAuth 2.0 PKCE"]
+        B1["User clicks Connect X<br/>(must already have github_user_id cookie)"] --> B2["GET /auth/x<br/>generatePKCEPair → verifier+challenge"]
+        B2 --> B3["store {verifier, githubUserId} under state<br/>(in-memory pkceStore)"]
+        B3 --> B4["Redirect to twitter.com/i/oauth2/authorize<br/>scopes: tweet.read/write users.read offline.access<br/>+ code_challenge (S256)"]
+        B4 --> B5["X callback → GET /auth/x/callback?code,state"]
+        B5 --> B6{"pkceStore has state?"}
+        B6 -- no --> B7["Render error page"]
+        B6 -- yes --> B8["exchangeCodeForTokens(code, verifier)<br/>POST api.x.com/2/oauth2/token"]
+        B8 --> B9["storeOAuthToken keyed by github:{id}<br/>incl. refresh_token + expires_at"]
+        B9 --> B10["Render success page (auto-closes)"]
+    end
+```
+
+Both flows use CSRF `state` (GitHub: `githubStateStore`; X: encoded into `x_{ts}_{githubUserId}` and held in `pkceStore`), with 10-minute cleanup of stale entries. X tokens are stored **per GitHub user** (`getXOAuthUserId` → `github:{id}`), which is what makes the product multi-user: each developer posts to their own X account.
+
+---
+
+## Data model
+
+sql.js SQLite, schema defined in `database.js:78-219`. All tables key off a string `user_id` of the form `github:{githubUserId}` (`'default'` for legacy/single-user paths).
+
+```mermaid
+erDiagram
+    users ||--o{ user_repos : owns
+    users ||--o{ user_prompt_templates : configures
+    users ||--o{ api_usage : accrues
+    user_repos }o--|| repo_contexts : "cached for"
+    user_repos }o--|| og_posts : "may pin"
+    user_repos ||--o{ tweets : "produces"
+
+    users {
+        text user_id PK "github:{id}"
+        text github_username
+        text display_name
+        text tier "free|pro|enterprise"
+        int  api_quota_limit
+    }
+    user_repos {
+        text user_id FK
+        text repo_full_name
+        text webhook_secret "per-repo HMAC secret"
+        int  is_active "enabled flag"
+    }
+    repo_contexts {
+        text repo_full_name PK
+        text context_json "languages, frameworks, readme summary"
+        datetime last_updated "24h staleness"
+    }
+    oauth_tokens {
+        text user_id "X token owner"
+        text token "JSON access token"
+        real expires_at
+        text refresh_token
+    }
+    github_tokens {
+        text github_user_id PK
+        text access_token
+        text refresh_token
+        text user_json
+        datetime expires_at "null = classic OAuth"
+    }
+    tweets {
+        text user_id
+        text repo_name
+        text commit_sha UK
+        text tweet_id
+        text tweet_text
+        text status "default posted"
+        text author
+    }
+    og_posts {
+        text repo_name PK
+        text tweet_id "quote target"
+    }
+    api_usage {
+        text user_id
+        text endpoint "gemini"
+        int  request_count
+        datetime period_start "hourly bucket"
+    }
+    user_prompt_templates {
+        text user_id
+        text template_id
+        text template_content
+        int  is_active
+    }
+    queue_items {
+        text queue_id UK
+        text task_type "gemini_changelog | ..."
+        text data_json "serialized task payload"
+        int  priority "1 high .. 3 low"
+        text status "pending|processing|retrying|completed|failed"
+        int  retry_count
+    }
+```
+
+| Table | Purpose | Key columns |
+| --- | --- | --- |
+| `users` | Account + tier/quota record. | `user_id` (unique), `tier`, `api_quota_limit` |
+| `user_repos` | Which repos a user enabled; carries the per-repo webhook secret and the active toggle. | `(user_id, repo_full_name)` unique, `webhook_secret`, `is_active` |
+| `repo_contexts` | Cached AI context (stack, frameworks, README summary) generated from webhook data; 24h staleness window. | `repo_full_name` unique, `context_json`, `last_updated` |
+| `oauth_tokens` | Per-user **X** OAuth tokens (access + refresh + expiry). | `user_id`, `token`, `expires_at`, `refresh_token` |
+| `github_tokens` | Persistent **GitHub** tokens with refresh support; survives restarts. | `github_user_id` unique, `access_token`, `refresh_token`, `expires_at` |
+| `tweets` | Ledger of posted tweets (commit→tweet mapping). `tweet_text`/`status`/`author` added by additive migration. | `commit_sha` unique, `tweet_id`, `repo_name` |
+| `og_posts` | One pinned "OG" tweet per repo, quoted by every changelog tweet. | `repo_name` unique, `tweet_id` |
+| `api_usage` | Hourly Gemini call counter per user for quota enforcement. | `user_id`, `endpoint`, `request_count`, `period_start` |
+| `user_prompt_templates` | Custom/preset prompt templates; one active per user. | `(user_id, template_id)` unique, `is_active` |
+| `queue_items` | Durable mirror of the in-memory Gemini queue so work resumes after restart. | `queue_id` unique, `task_type`, `data_json`, `status`, `retry_count` |
+
+---
+
+## Failure modes & known weaknesses
+
+- **HMAC fails OPEN when no secret is configured.** `verifyGitHubSignature` returns `true` (and only logs a warning) if neither a per-repo secret nor `WEBHOOK_SECRET` is set (`webhookHandler.js:32-35`). With no secret, anyone who can reach `/webhook/github` can forge a push payload and trigger tweets. **Always set `WEBHOOK_SECRET`** (and note that webhook *creation* via `createWebhook` already refuses to proceed without one — `server.js:438`). With a secret set, signatures are compared with `crypto.timingSafeEqual`.
+- **Live network calls in the hot path.** Each commit makes three external calls — GitHub diff fetch, Gemini Stage 1, Gemini Stage 2 (and then X). Any of them can be slow, rate-limited, or down. The webhook is processed **synchronously before responding to GitHub**, so a slow upstream delays the HTTP response; very large pushes amplify this linearly.
+- **Queue retry behavior.** Gemini work runs through `queueService`: capped at `QUEUE_MAX_RPM` requests/minute, retried up to `QUEUE_MAX_RETRIES` times with jittered exponential backoff (`QUEUE_BASE_RETRY_DELAY × 2^n`, capped at `QUEUE_MAX_RETRY_DELAY`; rate-limit errors get a ≥30s floor). Items are persisted in `queue_items` and restored on startup (`processing` items are reset to `pending`). After max retries an item is marked `failed` and dropped. Note Stage-1 `analyzeDiff` is **not** queued — it calls Gemini directly — so it isn't rate-limit-protected the way Stage 2 is.
+- **GitHub diff fetch degradation.** On 404/403/error, `fetchCommitDiff` returns `{ error }` and the pipeline falls back to a file-name heuristic summary (`buildFileBasedSummary`) — tweets still go out, but grounded only in filenames, which can read as vague.
+- **Diff analysis is skipped** for assets-only commits and for pushes touching >50 files (`shouldSkipDiffAnalysis`), again falling back to the file-name summary.
+- **In-memory OAuth state.** `pkceStore` and `githubStateStore` are plain `Map`s in process memory. A restart mid-flow (or running multiple instances behind a load balancer) loses pending OAuth state and breaks the callback. The design assumes a **single process**.
+- **sql.js durability window.** The database lives in WASM memory and is flushed to disk every 30s (and on graceful shutdown). A hard crash (e.g. `SIGKILL`, power loss) can lose up to ~30s of writes. sql.js is also single-process — concurrent writers would corrupt state.
+- **GitHub API proxy uses "the first token it finds."** `/api/github/proxy/*` picks `getAllGithubTokens()[0]` (`server.js:1004-1009`); it is a single-user convenience and not user-scoped.
+
+---
+
+## Local development
+
+> The README owns the full quick-start; this is the pointer.
+
+```bash
+pnpm setup           # installs backend + frontend deps
+pnpm seed            # populate tweets.db with demo fixtures (scripts/seed.js)
+pnpm dev:all         # backend (nodemon) + Vite dev server together
+pnpm test            # backend Vitest suite (webhook E2E + queue mechanics)
+```
+
+Required environment (`.env`, surfaced through `config/config.js`):
+
+- **GitHub:** `GITHUB_CLIENT_ID`, `GITHUB_CLIENT_SECRET`, `WEBHOOK_SECRET` (do not skip — see failure modes), `ALLOWED_REPOS` (optional legacy allowlist).
+- **X:** `OAUTH_CLIENT_ID` (required), `OAUTH_CLIENT_SECRET` (optional, enables confidential-client mode), `OAUTH_CALLBACK_URL`.
+- **Gemini:** `GEMINI_API_KEY`, `GEMINI_MODEL` (default `gemini-2.0-flash`).
+- **Queue:** `QUEUE_MAX_RPM` (default 15), `QUEUE_MAX_RETRIES` (3), `QUEUE_BASE_RETRY_DELAY` (2000ms), `QUEUE_MAX_RETRY_DELAY` (60000ms).
+- **Server:** `PORT` (3000), `NODE_ENV`, `FRONTEND_URL`, `API_BASE_URL`, `DATABASE_PATH` (default `./tweets.db`), `ADMIN_API_KEY` (gates `/api/*` admin + proxy), `VITE_API_BASE` (frontend).
diff --git a/package.json b/package.json
index 22d9229..a69bfad 100644
--- a/package.json
+++ b/package.json
@@ -6,30 +6,31 @@
   "scripts": {
     "start": "node src/server.js",
     "dev": "nodemon src/server.js",
-    "dev:all": "concurrently \"npm run dev\" \"npm run frontend:dev\"",
-    "frontend:dev": "cd frontend && npm run dev",
-    "frontend:build": "cd frontend && npm run build",
-    "frontend:install": "cd frontend && npm install",
-    "setup": "npm install && npm run frontend:install",
-    "build": "npm run frontend:build",
-    "test:local": "node test-local.js",
-    "test:webhook": "node test-webhook.js",
-    "test:gemini": "node test-gemini.js",
-    "index-repo": "node src/repoIndexer.js",
-    "seed": "node scripts/seed.js"
+    "dev:all": "concurrently \"pnpm dev\" \"pnpm frontend:dev\"",
+    "frontend:dev": "pnpm --dir frontend dev",
+    "frontend:build": "pnpm --dir frontend build",
+    "frontend:install": "pnpm --dir frontend install",
+    "setup": "pnpm install && pnpm frontend:install",
+    "build": "pnpm frontend:build",
+    "test": "vitest run",
+    "test:watch": "vitest",
+    "seed": "node scripts/seed.js",
+    "index-repo": "node src/repoIndexer.js"
   },
   "dependencies": {
-    "express": "^4.21.2",
-    "twitter-api-sdk": "^1.2.1",
     "@google/generative-ai": "^0.21.0",
+    "cookie-parser": "^1.4.6",
+    "cors": "^2.8.5",
     "dotenv": "^16.4.7",
+    "express": "^4.21.2",
     "sql.js": "^1.11.0",
-    "cors": "^2.8.5",
-    "cookie-parser": "^1.4.6"
+    "twitter-api-sdk": "^1.2.1"
   },
   "devDependencies": {
     "concurrently": "^8.2.2",
-    "nodemon": "^3.1.9"
+    "nodemon": "^3.1.9",
+    "supertest": "^7.2.2",
+    "vitest": "^2.1.9"
   },
   "engines": {
     "node": ">=18.0.0"
@@ -45,4 +46,3 @@
     "automation"
   ]
 }
-
diff --git a/pnpm-workspace.yaml b/pnpm-workspace.yaml
new file mode 100644
index 0000000..40bc527
--- /dev/null
+++ b/pnpm-workspace.yaml
@@ -0,0 +1,5 @@
+# pnpm requires native build scripts to be explicitly allowlisted.
+# esbuild (used by vitest/vite) needs its postinstall to fetch the native binary,
+# otherwise the test runner fails on a clean clone (especially on Windows).
+allowBuilds:
+  esbuild: true
diff --git a/src/server.js b/src/server.js
index 3549685..41c6ec1 100644
--- a/src/server.js
+++ b/src/server.js
@@ -1147,76 +1147,91 @@ app.use((err, req, res, next) => {
   });
 });
 
-const PORT = config.server.port;
-const server = app.listen(PORT, () => {
-  console.log(`🚀 Git→X Bot listening on port ${PORT}`);
-  console.log(`🌐 Frontend: ${FRONTEND_URL}`);
-  console.log(`📡 Webhook: /webhook/github`);
-  console.log(`🔐 Auth endpoints:`);
-  if (config.github.clientId) {
-    console.log(`   GitHub: /auth/github → /auth/github/callback`);
-  }
-  if (config.twitter.clientId) {
-    console.log(`   X:      /auth/x → /auth/x/callback`);
-  }
-  console.log(`📊 API: /api/me, /api/me/repos`);
-  console.log(`🔒 Webhook secret: ${config.github.webhookSecret ? 'SET' : 'NOT SET'}`);
-  console.log(`💾 Queue persistence: enabled (survives restarts)`);
-});
-
 // ============================================
-// Graceful Shutdown Handling
+// Server bootstrap (only when run directly)
 // ============================================
+// Exporting `app` lets tests (supertest) import the wired Express app WITHOUT
+// binding a port or installing process-level signal handlers. The listen/shutdown
+// block runs only when this file is executed directly (`node src/server.js`).
+
+function startServer() {
+  const PORT = config.server.port;
+  const server = app.listen(PORT, () => {
+    console.log(`🚀 Git→X Bot listening on port ${PORT}`);
+    console.log(`🌐 Frontend: ${FRONTEND_URL}`);
+    console.log(`📡 Webhook: /webhook/github`);
+    console.log(`🔐 Auth endpoints:`);
+    if (config.github.clientId) {
+      console.log(`   GitHub: /auth/github → /auth/github/callback`);
+    }
+    if (config.twitter.clientId) {
+      console.log(`   X:      /auth/x → /auth/x/callback`);
+    }
+    console.log(`📊 API: /api/me, /api/me/repos`);
+    console.log(`🔒 Webhook secret: ${config.github.webhookSecret ? 'SET' : 'NOT SET'}`);
+    console.log(`💾 Queue persistence: enabled (survives restarts)`);
+  });
 
-let isShuttingDown = false;
+  let isShuttingDown = false;
 
-async function gracefulShutdown(signal) {
-  if (isShuttingDown) {
-    console.log('⚠️  Shutdown already in progress...');
-    return;
-  }
-  
-  isShuttingDown = true;
-  console.log(`\n🛑 Received ${signal}, starting graceful shutdown...`);
-  
-  // Stop accepting new connections
-  server.close((err) => {
-    if (err) {
-      console.error('❌ Error closing server:', err.message);
-    } else {
-      console.log('✅ Server closed - no new connections');
+  async function gracefulShutdown(signal) {
+    if (isShuttingDown) {
+      console.log('⚠️  Shutdown already in progress...');
+      return;
+    }
+
+    isShuttingDown = true;
+    console.log(`\n🛑 Received ${signal}, starting graceful shutdown...`);
+
+    // Stop accepting new connections
+    server.close((err) => {
+      if (err) {
+        console.error('❌ Error closing server:', err.message);
+      } else {
+        console.log('✅ Server closed - no new connections');
+      }
+    });
+
+    try {
+      // Stop queue service (persists pending items)
+      console.log('💾 Stopping queue service...');
+      shutdownQueueService();
+
+      // Close database connection (saves and closes)
+      console.log('💾 Closing database...');
+      database.closeDatabase();
+
+      console.log('✅ Graceful shutdown complete');
+      process.exit(0);
+    } catch (error) {
+      console.error('❌ Error during shutdown:', error.message);
+      process.exit(1);
     }
-  });
-  
-  try {
-    // Stop queue service (persists pending items)
-    console.log('💾 Stopping queue service...');
-    shutdownQueueService();
-    
-    // Close database connection (saves and closes)
-    console.log('💾 Closing database...');
-    database.closeDatabase();
-    
-    console.log('✅ Graceful shutdown complete');
-    process.exit(0);
-  } catch (error) {
-    console.error('❌ Error during shutdown:', error.message);
-    process.exit(1);
   }
-}
 
-// Handle different shutdown signals
-process.on('SIGTERM', () => gracefulShutdown('SIGTERM'));
-process.on('SIGINT', () => gracefulShutdown('SIGINT'));
+  // Handle different shutdown signals
+  process.on('SIGTERM', () => gracefulShutdown('SIGTERM'));
+  process.on('SIGINT', () => gracefulShutdown('SIGINT'));
 
-// Handle uncaught exceptions gracefully
-process.on('uncaughtException', (error) => {
-  console.error('❌ Uncaught Exception:', error);
-  gracefulShutdown('uncaughtException');
-});
+  // Handle uncaught exceptions gracefully
+  process.on('uncaughtException', (error) => {
+    console.error('❌ Uncaught Exception:', error);
+    gracefulShutdown('uncaughtException');
+  });
 
-process.on('unhandledRejection', (reason, promise) => {
-  console.error('❌ Unhandled Rejection at:', promise, 'reason:', reason);
-  // Don't exit on unhandled rejections, just log them
-});
+  process.on('unhandledRejection', (reason, promise) => {
+    console.error('❌ Unhandled Rejection at:', promise, 'reason:', reason);
+    // Don't exit on unhandled rejections, just log them
+  });
+
+  return server;
+}
+
+if (require.main === module) {
+  startServer();
+}
+
+module.exports = app;
+module.exports.app = app;
+module.exports.startServer = startServer;
 
diff --git a/tests/fixtures/push-event.json b/tests/fixtures/push-event.json
new file mode 100644
index 0000000..1221ffa
--- /dev/null
+++ b/tests/fixtures/push-event.json
@@ -0,0 +1,32 @@
+{
+  "ref": "refs/heads/main",
+  "before": "0000000000000000000000000000000000000000",
+  "after": "a1b2c3d4e5f60718293abc4d5e6f708192a3b4c5",
+  "repository": {
+    "id": 900000001,
+    "name": "payments-api",
+    "full_name": "octo-dev/payments-api",
+    "private": false,
+    "owner": { "name": "octo-dev", "login": "octo-dev", "id": 90000001 },
+    "html_url": "https://github.com/octo-dev/payments-api",
+    "description": "Payments API",
+    "default_branch": "main"
+  },
+  "pusher": { "name": "octo-dev", "email": "octo-dev@fixtures.invalid" },
+  "sender": { "login": "octo-dev", "id": 90000001 },
+  "commits": [
+    {
+      "id": "a1b2c3d4e5f60718293abc4d5e6f708192a3b4c5",
+      "tree_id": "ffeeddccbbaa00998877665544332211aabbccdd",
+      "distinct": true,
+      "message": "feat: add idempotency keys to POST /charges to prevent double-charging on retries",
+      "timestamp": "2026-05-20T09:14:00Z",
+      "url": "https://github.com/octo-dev/payments-api/commit/a1b2c3d",
+      "author": { "name": "octo-dev", "email": "octo-dev@fixtures.invalid", "username": "octo-dev" },
+      "committer": { "name": "octo-dev", "email": "octo-dev@fixtures.invalid", "username": "octo-dev" },
+      "added": ["src/middleware/idempotency.js"],
+      "removed": [],
+      "modified": ["src/routes/charges.js", "src/db/schema.sql"]
+    }
+  ]
+}
diff --git a/tests/queue.test.js b/tests/queue.test.js
new file mode 100644
index 0000000..0baf63d
--- /dev/null
+++ b/tests/queue.test.js
@@ -0,0 +1,64 @@
+import { describe, it, expect, beforeEach, afterEach, vi } from 'vitest';
+import os from 'node:os';
+import path from 'node:path';
+
+// Isolated temp DB for the queue's auto-init (persistence is disabled per-instance
+// below so these tests don't depend on DB writes).
+process.env.NODE_ENV = 'test';
+process.env.DATABASE_PATH = path.join(os.tmpdir(), `gitlogs-queue-${process.pid}.db`);
+
+const queueMod = await import('../src/queueService');
+const QueueService = queueMod.QueueService || queueMod.default.QueueService;
+
+describe('QueueService — rate-limited retry queue', () => {
+  let qs;
+
+  beforeEach(() => {
+    qs = new QueueService({
+      processingIntervalMs: 10,
+      maxRetries: 2,
+      baseRetryDelayMs: 5,
+      maxRetryDelayMs: 40,
+      maxRequestsPerMinute: 1000,
+    });
+    qs.persistenceEnabled = false; // pure in-memory mechanics under test
+    qs.start();
+  });
+
+  afterEach(() => {
+    qs.stop();
+  });
+
+  it('processes a task and resolves with its result', async () => {
+    const task = vi.fn().mockResolvedValue('done');
+    const result = await qs.enqueue({ task, taskType: 'unit-success' });
+
+    expect(result).toBe('done');
+    expect(task).toHaveBeenCalledTimes(1);
+    expect(qs.stats.totalProcessed).toBe(1);
+  });
+
+  it('retries with backoff and eventually succeeds', async () => {
+    let attempts = 0;
+    const task = vi.fn().mockImplementation(async () => {
+      attempts += 1;
+      if (attempts < 3) throw new Error('transient failure');
+      return 'recovered';
+    });
+
+    const result = await qs.enqueue({ task, taskType: 'unit-retry' });
+
+    expect(result).toBe('recovered');
+    expect(attempts).toBe(3); // 1 initial + 2 retries
+    expect(qs.stats.totalRetries).toBe(2);
+    expect(qs.stats.totalProcessed).toBe(1);
+  });
+
+  it('gives up after maxRetries and rejects', async () => {
+    const task = vi.fn().mockRejectedValue(new Error('permanent failure'));
+
+    await expect(qs.enqueue({ task, taskType: 'unit-fail' })).rejects.toThrow('permanent failure');
+    expect(task).toHaveBeenCalledTimes(3); // 1 initial + 2 retries
+    expect(qs.stats.totalFailed).toBe(1);
+  });
+});
diff --git a/tests/webhook.e2e.test.js b/tests/webhook.e2e.test.js
new file mode 100644
index 0000000..bb6d184
--- /dev/null
+++ b/tests/webhook.e2e.test.js
@@ -0,0 +1,129 @@
+import { describe, it, expect, beforeAll, afterAll, vi } from 'vitest';
+import crypto from 'node:crypto';
+import fs from 'node:fs';
+import path from 'node:path';
+import { fileURLToPath } from 'node:url';
+import { createRequire } from 'node:module';
+
+// Load the whole backend through ONE Node CommonJS require graph. This is the
+// reliable seam: vitest's vi.mock does NOT intercept transitive require()s of
+// CJS modules here, but because every module below shares the SAME require
+// cache, the stub functions we assign are the exact instances webhookHandler
+// holds by reference — so overriding them controls the real pipeline.
+//
+// Env (WEBHOOK_SECRET, ALLOWED_REPOS, ENABLE_THREADING, DATABASE_PATH) is set in
+// vitest.config.js, so it's already in process.env before these modules load.
+const require = createRequire(import.meta.url);
+const requestFactory = require('supertest');
+const request = requestFactory.default || requestFactory;
+
+const geminiClient = require('../src/geminiClient');
+const twitterClient = require('../src/twitterClient');
+const diffAnalyzer = require('../src/diffAnalyzer');
+const repoIndexer = require('../src/repoIndexer');
+const database = require('../src/database');
+const { shutdownQueueService } = require('../src/queueService');
+const app = require('../src/server');
+
+const TEST_SECRET = 'test-webhook-secret-e2e';
+const TEST_REPO = 'octo-dev/payments-api';
+const GENERATED_TWEET =
+  'shipped idempotency keys on POST /charges so retries never double-charge';
+const POSTED_TWEET_ID = '1799999999999999999';
+
+// ── Stub the external network boundaries (Gemini, X, GitHub diff, indexer) ──
+// Everything BETWEEN the HTTP edge and these stubs (HMAC verify, parsing,
+// repo/user resolution, formatting, persistence) runs for real.
+geminiClient.analyzeDiff = vi.fn().mockResolvedValue('- Adds idempotency middleware to POST /charges');
+geminiClient.generateChangelog = vi.fn().mockResolvedValue(GENERATED_TWEET);
+geminiClient.getQueueStats = vi
+  .fn()
+  .mockReturnValue({ currentQueueLength: 0, totalProcessed: 1, rateLimitRemaining: 5 });
+
+twitterClient.postTweet = vi.fn().mockResolvedValue(POSTED_TWEET_ID);
+
+diffAnalyzer.fetchCommitDiff = vi.fn().mockResolvedValue({
+  diff: 'diff --git a/src/routes/charges.js b/src/routes/charges.js\n+ idempotency',
+  files: ['src/routes/charges.js'],
+  stats: { additions: 18, deletions: 3 },
+  error: null,
+});
+diffAnalyzer.shouldSkipDiffAnalysis = vi.fn().mockReturnValue(false);
+diffAnalyzer.buildFileBasedSummary = vi.fn().mockReturnValue('Updated src/routes/charges.js');
+
+repoIndexer.generateContextFromWebhook = vi
+  .fn()
+  .mockReturnValue({ languages: ['JavaScript'], description: 'Payments API' });
+
+const __dirname = path.dirname(fileURLToPath(import.meta.url));
+const PUSH_PAYLOAD = JSON.parse(
+  fs.readFileSync(path.join(__dirname, 'fixtures', 'push-event.json'), 'utf8')
+);
+const COMMIT_SHA = PUSH_PAYLOAD.commits[0].id;
+
+function sign(rawBody, secret = TEST_SECRET) {
+  return 'sha256=' + crypto.createHmac('sha256', secret).update(rawBody).digest('hex');
+}
+
+function postWebhook({ body, signature, event = 'push' }) {
+  return request(app)
+    .post('/webhook/github')
+    .set('content-type', 'application/json')
+    .set('x-github-event', event)
+    .set('x-hub-signature-256', signature)
+    .send(body); // send the EXACT signed string, not a re-serialized object
+}
+
+beforeAll(async () => {
+  await database.initDatabase();
+});
+
+afterAll(() => {
+  shutdownQueueService();
+  database.closeDatabase();
+  const dbPath = process.env.DATABASE_PATH;
+  if (dbPath && fs.existsSync(dbPath)) fs.unlinkSync(dbPath);
+});
+
+describe('POST /webhook/github — end-to-end pipeline', () => {
+  it('verifies HMAC, runs the AI pipeline, posts to X (stub), and persists the tweet', async () => {
+    const raw = JSON.stringify(PUSH_PAYLOAD);
+    const res = await postWebhook({ body: raw, signature: sign(raw) });
+
+    expect(res.status).toBe(200);
+    expect(res.body).toMatchObject({ status: 'OK', processed: 1, total: 1 });
+
+    // Gemini two-stage generation was invoked
+    expect(geminiClient.analyzeDiff).toHaveBeenCalledTimes(1);
+    expect(geminiClient.generateChangelog).toHaveBeenCalledTimes(1);
+
+    // X received exactly the generated changelog text
+    expect(twitterClient.postTweet).toHaveBeenCalledTimes(1);
+    const [postedText] = twitterClient.postTweet.mock.calls[0];
+    expect(postedText).toContain('idempotency');
+
+    // The tweet was persisted to the ledger
+    const persisted = await database.getTweetsForRepo(TEST_REPO);
+    const row = persisted.find((t) => t.commit_sha === COMMIT_SHA);
+    expect(row).toBeTruthy();
+    expect(row.tweet_id).toBe(POSTED_TWEET_ID);
+  });
+
+  it('rejects a tampered/invalid signature with 401 and never posts', async () => {
+    twitterClient.postTweet.mockClear();
+    const raw = JSON.stringify(PUSH_PAYLOAD);
+    const res = await postWebhook({ body: raw, signature: sign(raw, 'wrong-secret') });
+
+    expect(res.status).toBe(401);
+    expect(twitterClient.postTweet).not.toHaveBeenCalled();
+  });
+
+  it('ignores non-push events (e.g. ping) with 200 and never posts', async () => {
+    twitterClient.postTweet.mockClear();
+    const raw = JSON.stringify(PUSH_PAYLOAD);
+    const res = await postWebhook({ body: raw, signature: sign(raw), event: 'ping' });
+
+    expect(res.status).toBe(200);
+    expect(twitterClient.postTweet).not.toHaveBeenCalled();
+  });
+});
diff --git a/vitest.config.js b/vitest.config.js
new file mode 100644
index 0000000..d8ebad7
--- /dev/null
+++ b/vitest.config.js
@@ -0,0 +1,31 @@
+import os from 'node:os';
+import path from 'node:path';
+import { defineConfig } from 'vitest/config';
+
+// A throwaway DB path for the test run. Set here (not in the test file) so it is
+// in process.env BEFORE config/config.js + database.js read it at module load.
+const TEST_DB = path.join(os.tmpdir(), 'gitlogs-vitest.db');
+
+export default defineConfig({
+  test: {
+    environment: 'node',
+    include: ['tests/**/*.test.js'],
+    testTimeout: 20000,
+    hookTimeout: 20000,
+    // The backend uses module-level singletons (sql.js DB, the queue interval).
+    // Run in a single fork, files serially, so suites don't clobber shared state.
+    pool: 'forks',
+    poolOptions: { forks: { singleFork: true } },
+    fileParallelism: false,
+    // dotenv in config.js does NOT override already-set vars, so these win over
+    // the committed .env — tests never touch real secrets or live services.
+    env: {
+      NODE_ENV: 'test',
+      WEBHOOK_SECRET: 'test-webhook-secret-e2e',
+      ALLOWED_REPOS: 'octo-dev/payments-api',
+      ENABLE_THREADING: 'true',
+      DATABASE_PATH: TEST_DB,
+      GEMINI_API_KEY: 'test-key-not-used',
+    },
+  },
+});
</stdin>
hook: SessionStart
hook: SessionStart Failed
codex
I’ll review the patch against the actual repository where needed, not just the prose in the diff. First I’m loading the relevant review workflow skill, then I’ll verify the touched surfaces and return findings only.
exec
"C:\\Users\\Aayushman\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command 'Get-Content -Raw C:/Users/Aayushman/.codex/plugins/cache/claude-plugins-official/superpowers/5.1.0/skills/using-superpowers/SKILL.md' in C:\Repo\gitlogs
2026-06-08T15:01:20.807170Z ERROR codex_core::exec: exec error: windows sandbox: spawn setup refresh
 exited -1 in 0ms:
execution error: Io(Custom { kind: Other, error: "windows sandbox: spawn setup refresh" })
2026-06-08T15:01:20.810436Z ERROR codex_core::tools::router: error=execution error: Io(Custom { kind: Other, error: "windows sandbox: spawn setup refresh" })
codex
The skill file read hit a local sandbox spawn error, so I’m proceeding with the built-in review stance and will keep the scope to the supplied diff plus targeted repo checks.
exec
"C:\\Users\\Aayushman\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "rg -n \"module\\.exports|generateChangelog|analyzeDiff|initialize|queue|setInterval|process\\.on|app.listen|startServer\" src/geminiClient.js src/server.js src/database.js" in C:\Repo\gitlogs
exec
"C:\\Users\\Aayushman\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "rg -n \"function verifyGitHubSignature|verifyGitHubSignature|app.use\\(|express\\.json|rawBody|/webhook/github|module\\.exports|function getAllGithubTokens|shutdownQueueService|saveTweetId|getTweetsForRepo|getRepoWebhookSecret|getUserByRepo|isRepoEnabled\" src" in C:\Repo\gitlogs
exec
"C:\\Users\\Aayushman\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "rg -n \"class QueueService|constructor\\(|enqueue\\(|processQueue|processItem|persistenceEnabled|restore|setInterval|unref|maxRequestsPerMinute|rateLimit\" src/queueService.js" in C:\Repo\gitlogs
exec
"C:\\Users\\Aayushman\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command 'rg -n "vitest|type|scripts|dependencies|devDependencies|packageManager|engines" package.json frontend/package.json pnpm-workspace.yaml .github/workflows/ci.yml .github/workflows/deploy-ec2.yml' in C:\Repo\gitlogs
 succeeded in 552ms:
src\githubAuth.js:369:module.exports = {
src\diffAnalyzer.js:273:module.exports = {
src\oauthHandler.js:290:module.exports = OAuthHandler;
src\geminiClient.js:477:module.exports = {
src\repoIndexer.js:327:module.exports = {
src\pkceHelper.js:96:module.exports = {
src\templateEngine.js:475:module.exports = {
src\queueService.js:546:function shutdownQueueService() {
src\queueService.js:553:module.exports = {
src\queueService.js:556:  shutdownQueueService,
src\database.js:329:async function saveTweetId(repoName, commitSha, tweetId) {
src\database.js:340:async function getTweetsForRepo(repoName) {
src\database.js:350: * Unlike saveTweetId (the thin webhook write path that only records
src\database.js:651:function getUserByRepo(repoFullName) {
src\database.js:681:function getRepoWebhookSecret(repoFullName) {
src\database.js:718:function isRepoEnabled(repoFullName) {
src\database.js:843:function getAllGithubTokens() {
src\database.js:1192:module.exports = {
src\database.js:1198:  saveTweetId,
src\database.js:1200:  getTweetsForRepo,
src\database.js:1217:  getUserByRepo,
src\database.js:1220:  getRepoWebhookSecret,
src\database.js:1225:  isRepoEnabled,
src\twitterClient.js:360:module.exports = {
src\commitFormatter.js:264:module.exports = {
src\server.js:9:const { getQueueService, shutdownQueueService } = require('./queueService');
src\server.js:20:app.use(express.static(frontendPath, { fallthrough: true }));
src\server.js:21:app.use(express.static(publicPath, { fallthrough: true }));
src\server.js:29:app.use('/webhook/github', express.raw({ type: '*/*' }), (req, res, next) => {
src\server.js:30:  req.rawBody = req.body.toString('utf8');
src\server.js:35:app.use('/webhook/github', express.urlencoded({ extended: false }));
src\server.js:36:app.use('/webhook/github', express.json());
src\server.js:39:app.use(express.json());
src\server.js:42:app.use(cookieParser());
src\server.js:188:app.use(cors({
src\server.js:435:    const webhookUrl = `${process.env.API_BASE_URL || `http://localhost:${config.server.port}`}/webhook/github`;
src\server.js:483:    const webhookUrl = `${process.env.API_BASE_URL || `http://localhost:${config.server.port}`}/webhook/github`;
src\server.js:832:app.post('/webhook/github', webhookHandler.handleWebhook);
src\server.js:975:  const isEnabled = database.isRepoEnabled(repoFullName);
src\server.js:976:  const user = database.getUserByRepo(repoFullName);
src\server.js:1074:      webhook: '/webhook/github',
src\server.js:1126:app.use((req, res) => {
src\server.js:1142:app.use((err, req, res, next) => {
src\server.js:1162:    console.log(`📡 Webhook: /webhook/github`);
src\server.js:1198:      shutdownQueueService();
src\server.js:1234:module.exports = app;
src\server.js:1235:module.exports.app = app;
src\server.js:1236:module.exports.startServer = startServer;
src\webhookHandler.js:20:function verifyGitHubSignature(payload, signature, repoFullName = null) {
src\webhookHandler.js:24:    secret = database.getRepoWebhookSecret(repoFullName);
src\webhookHandler.js:59:  const isEnabled = database.isRepoEnabled(repoFullName);
src\webhookHandler.js:60:  console.log(`🔍 Checking repo ${repoFullName}: isRepoEnabled=${isEnabled}`);
src\webhookHandler.js:255:      await database.saveTweetId(repository.full_name, commit.id, tweetId);
src\webhookHandler.js:276:    let rawBody = req.rawBody;
src\webhookHandler.js:277:    if (Buffer.isBuffer(rawBody)) {
src\webhookHandler.js:278:      rawBody = rawBody.toString('utf8');
src\webhookHandler.js:285:      const parsed = querystring.parse(rawBody);
src\webhookHandler.js:293:        body = JSON.parse(rawBody);
src\webhookHandler.js:296:        console.error('Raw body preview:', rawBody.substring(0, 200));
src\webhookHandler.js:310:    if (!verifyGitHubSignature(rawBody, signature, repoFullName)) {
src\webhookHandler.js:351:    const user = database.getUserByRepo(repository.full_name);
src\webhookHandler.js:418:module.exports = {

 succeeded in 576ms:
45:class QueueService {
46:  constructor(options = {}) {
48:    this.maxRequestsPerMinute = options.maxRequestsPerMinute || config.queue?.maxRequestsPerMinute || 15;
67:      restoredFromDb: 0
79:    this.persistenceEnabled = true;
81:    console.log(`📋 Queue service initialized (rate: ${this.maxRequestsPerMinute} req/min, retries: ${this.maxRetries}, persistence: enabled)`);
94:    this.restoreFromDatabase();
97:    this.cleanupInterval = setInterval(() => {
101:    this.processingInterval = setInterval(() => this.processQueue(), this.processingIntervalMs);
137:  restoreFromDatabase() {
146:        console.log('📋 No pending queue items to restore');
152:      let restored = 0;
181:            restored: true // Flag to indicate this was restored
192:          restored++;
194:          console.error(`❌ Failed to restore queue item ${item.id}:`, err.message);
199:      this.stats.restoredFromDb = restored;
201:      console.log(`✅ Restored ${restored} queue items`);
204:      console.error('❌ Failed to restore queue from database:', error.message);
213:  enqueue(options) {
261:      if (this.persistenceEnabled && taskType) {
294:  async processQueue() {
302:    if (this.requestTimestamps.length >= this.maxRequestsPerMinute) {
315:    if (this.persistenceEnabled) {
338:      if (this.persistenceEnabled) {
344:      // For restored items, resolve may not be set
368:        if (this.persistenceEnabled) {
393:        if (this.persistenceEnabled) {
399:        // For restored items, reject may not be set
412:    const rateLimitIndicators = [
419:    return rateLimitIndicators.some(indicator => message.includes(indicator));
499:      rateLimitRemaining: Math.max(0, this.maxRequestsPerMinute - this.requestTimestamps.length)

 succeeded in 598ms:
.github/workflows/ci.yml:36:          find src scripts -name '*.js' -print0 | xargs -0 -n1 node --check
pnpm-workspace.yaml:1:# pnpm requires native build scripts to be explicitly allowlisted.
pnpm-workspace.yaml:2:# esbuild (used by vitest/vite) needs its postinstall to fetch the native binary,
frontend/package.json:4:  "type": "module",
frontend/package.json:5:  "scripts": {
frontend/package.json:10:  "dependencies": {
frontend/package.json:18:  "devDependencies": {
package.json:6:  "scripts": {
package.json:15:    "test": "vitest run",
package.json:16:    "test:watch": "vitest",
package.json:17:    "seed": "node scripts/seed.js",
package.json:20:  "dependencies": {
package.json:29:  "devDependencies": {
package.json:33:    "vitest": "^2.1.9"
package.json:35:  "engines": {

 succeeded in 623ms:
src/geminiClient.js:13:const { getQueueService, registerTaskType, PRIORITY } = require('./queueService');
src/geminiClient.js:18:// Task types for queue persistence
src/geminiClient.js:28:let queueService = null;
src/geminiClient.js:43:    throw new Error('Gemini model not initialized');
src/geminiClient.js:115:async function analyzeDiff(diffData, commitMessage, repoName, userId = 'default') {
src/geminiClient.js:285:    // Initialize queue service for rate limiting
src/geminiClient.js:286:    queueService = getQueueService({
src/geminiClient.js:287:      maxRequestsPerMinute: config.queue?.maxRequestsPerMinute || 15,
src/geminiClient.js:288:      maxRetries: config.queue?.maxRetries || 3
src/geminiClient.js:291:    // Register task factories for queue persistence
src/geminiClient.js:295:    console.log(`✅ Gemini AI initialized with model: ${modelName}`);
src/geminiClient.js:296:    console.log(`📝 Registered ${Object.keys(TASK_TYPES).length} task types for queue persistence`);
src/geminiClient.js:299:    console.error('❌ Failed to initialize Gemini:', error.message);
src/geminiClient.js:309: * Generate changelog with queue support and enhanced context
src/geminiClient.js:317:async function generateChangelog(commitData, repository, options = {}) {
src/geminiClient.js:358:  // Use queue service if available
src/geminiClient.js:359:  if (queueService) {
src/geminiClient.js:361:      const result = await queueService.enqueue({
src/geminiClient.js:385: * Generate detailed changelog with queue support
src/geminiClient.js:425:  // Use queue service if available
src/geminiClient.js:426:  if (queueService) {
src/geminiClient.js:428:      return await queueService.enqueue({
src/geminiClient.js:455: * Get queue statistics for monitoring
src/geminiClient.js:458:  if (!queueService) return null;
src/geminiClient.js:459:  return queueService.getStats();
src/geminiClient.js:466:  if (!queueService) return -1;
src/geminiClient.js:467:  return queueService.getUserQuotaRemaining(userId);
src/geminiClient.js:477:module.exports = {
src/geminiClient.js:479:  generateChangelog,
src/geminiClient.js:484:  analyzeDiff,
src/server.js:9:const { getQueueService, shutdownQueueService } = require('./queueService');
src/server.js:856:// Get queue and system stats
src/server.js:859:  const queueService = getQueueService();
src/server.js:864:    queue: queueService ? queueService.getStats() : null,
src/server.js:1045:  const queueService = getQueueService();
src/server.js:1046:  const queueStats = queueService ? queueService.getStats() : null;
src/server.js:1053:      queueEnabled: !!queueService,
src/server.js:1057:    queue: queueStats ? {
src/server.js:1058:      pending: queueStats.currentQueueLength,
src/server.js:1059:      processing: queueStats.processingCount,
src/server.js:1060:      rateLimitRemaining: queueStats.rateLimitRemaining
src/server.js:1157:function startServer() {
src/server.js:1159:  const server = app.listen(PORT, () => {
src/server.js:1196:      // Stop queue service (persists pending items)
src/server.js:1197:      console.log('💾 Stopping queue service...');
src/server.js:1213:  process.on('SIGTERM', () => gracefulShutdown('SIGTERM'));
src/server.js:1214:  process.on('SIGINT', () => gracefulShutdown('SIGINT'));
src/server.js:1217:  process.on('uncaughtException', (error) => {
src/server.js:1222:  process.on('unhandledRejection', (reason, promise) => {
src/server.js:1231:  startServer();
src/server.js:1234:module.exports = app;
src/server.js:1235:module.exports.app = app;
src/server.js:1236:module.exports.startServer = startServer;
src/database.js:201:        -- Persistent queue for surviving server restarts
src/database.js:202:        CREATE TABLE IF NOT EXISTS queue_items (
src/database.js:204:          queue_id TEXT UNIQUE NOT NULL,
src/database.js:216:        CREATE INDEX IF NOT EXISTS idx_queue_status ON queue_items(status);
src/database.js:217:        CREATE INDEX IF NOT EXISTS idx_queue_priority ON queue_items(priority, created_at);
src/database.js:218:        CREATE INDEX IF NOT EXISTS idx_queue_user ON queue_items(user_id);
src/database.js:244:      saveInterval = setInterval(saveDatabase, 30000);
src/database.js:247:      console.log('✅ Database initialized with sql.js (ARM64 compatible)');
src/database.js:1029: * Save a queue item to the database for persistence
src/database.js:1036:    `INSERT INTO queue_items (queue_id, task_type, user_id, data_json, priority, status, retry_count, updated_at)
src/database.js:1038:     ON CONFLICT(queue_id) DO UPDATE SET
src/database.js:1058: * Update queue item status
src/database.js:1059: * @param {string} queueId - Queue item ID
src/database.js:1063:function updateQueueItemStatus(queueId, status, errorMessage = null) {
src/database.js:1068:      'UPDATE queue_items SET status = ?, error_message = ?, updated_at = CURRENT_TIMESTAMP WHERE queue_id = ?',
src/database.js:1069:      [status, errorMessage, queueId]
src/database.js:1074:    'UPDATE queue_items SET status = ?, updated_at = CURRENT_TIMESTAMP WHERE queue_id = ?',
src/database.js:1075:    [status, queueId]
src/database.js:1080: * Increment retry count for a queue item
src/database.js:1081: * @param {string} queueId - Queue item ID
src/database.js:1083:function incrementQueueItemRetry(queueId) {
src/database.js:1087:    'UPDATE queue_items SET retry_count = retry_count + 1, status = ?, updated_at = CURRENT_TIMESTAMP WHERE queue_id = ?',
src/database.js:1088:    ['retrying', queueId]
src/database.js:1093: * Get all pending/retrying queue items (for restoration on startup)
src/database.js:1094: * @returns {Array} - Array of queue items
src/database.js:1100:    `SELECT * FROM queue_items 
src/database.js:1106:    id: row.queue_id,
src/database.js:1119: * Delete a queue item (after completion or final failure)
src/database.js:1120: * @param {string} queueId - Queue item ID
src/database.js:1122:function deleteQueueItem(queueId) {
src/database.js:1125:  return run('DELETE FROM queue_items WHERE queue_id = ?', [queueId]);
src/database.js:1135:  const count = getOne('SELECT COUNT(*) as count FROM queue_items WHERE status = ?', ['processing']);
src/database.js:1137:    console.log(`🔄 Resetting ${count.count} processing queue items to pending`);
src/database.js:1139:      'UPDATE queue_items SET status = ?, updated_at = CURRENT_TIMESTAMP WHERE status = ?',
src/database.js:1147: * Clean up old completed/failed queue items (older than 24 hours)
src/database.js:1154:    'DELETE FROM queue_items WHERE status IN (?, ?) AND updated_at < ?',
src/database.js:1160: * Get queue statistics
src/database.js:1165:  const pending = getOne('SELECT COUNT(*) as count FROM queue_items WHERE status = ?', ['pending']);
src/database.js:1166:  const processing = getOne('SELECT COUNT(*) as count FROM queue_items WHERE status = ?', ['processing']);
src/database.js:1167:  const retrying = getOne('SELECT COUNT(*) as count FROM queue_items WHERE status = ?', ['retrying']);
src/database.js:1168:  const failed = getOne('SELECT COUNT(*) as count FROM queue_items WHERE status = ?', ['failed']);
src/database.js:1192:module.exports = {

codex
I’ve confirmed the patch touches live bootstrapping, CI/deploy, and tests. Now I’m checking the real handler/queue/database behavior because several claims in the new architecture doc look like they may contradict the code paths.
exec
"C:\\Users\\Aayushman\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command 'Get-Content src/webhookHandler.js | Select-Object -First 440' in C:\Repo\gitlogs
exec
"C:\\Users\\Aayushman\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command 'Get-Content src/queueService.js | Select-Object -First 460' in C:\Repo\gitlogs
exec
"C:\\Users\\Aayushman\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command 'Get-Content src/database.js | Select-Object -Skip 40 -First 210' in C:\Repo\gitlogs
exec
"C:\\Users\\Aayushman\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command 'Get-Content src/server.js | Select-Object -First 70; Get-Content src/server.js | Select-Object -Skip 1138 -First 105' in C:\Repo\gitlogs
2026-06-08T15:01:38.647754Z ERROR codex_core::exec: exec error: windows sandbox: spawn setup refresh
 exited -1 in 0ms:
execution error: Io(Custom { kind: Other, error: "windows sandbox: spawn setup refresh" })
2026-06-08T15:01:38.651157Z ERROR codex_core::tools::router: error=execution error: Io(Custom { kind: Other, error: "windows sandbox: spawn setup refresh" })
2026-06-08T15:01:38.662169Z ERROR codex_core::exec: exec error: windows sandbox: spawn setup refresh
 exited -1 in 0ms:
execution error: Io(Custom { kind: Other, error: "windows sandbox: spawn setup refresh" })
2026-06-08T15:01:38.669942Z ERROR codex_core::tools::router: error=execution error: Io(Custom { kind: Other, error: "windows sandbox: spawn setup refresh" })
2026-06-08T15:01:38.675697Z ERROR codex_core::exec: exec error: windows sandbox: spawn setup refresh
 exited -1 in 0ms:
execution error: Io(Custom { kind: Other, error: "windows sandbox: spawn setup refresh" })
2026-06-08T15:01:38.683760Z ERROR codex_core::tools::router: error=execution error: Io(Custom { kind: Other, error: "windows sandbox: spawn setup refresh" })
2026-06-08T15:01:38.693554Z ERROR codex_core::exec: exec error: windows sandbox: spawn setup refresh
 exited -1 in 0ms:
execution error: Io(Custom { kind: Other, error: "windows sandbox: spawn setup refresh" })
2026-06-08T15:01:38.695527Z ERROR codex_core::tools::router: error=execution error: Io(Custom { kind: Other, error: "windows sandbox: spawn setup refresh" })
codex
The parallel reads are being rejected by the local sandbox wrapper, so I’m switching to smaller single reads.
exec
"C:\\Users\\Aayushman\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command 'Get-Content src/webhookHandler.js | Select-Object -First 440' in C:\Repo\gitlogs
2026-06-08T15:01:42.279360Z ERROR codex_core::exec: exec error: windows sandbox: spawn setup refresh
 exited -1 in 0ms:
execution error: Io(Custom { kind: Other, error: "windows sandbox: spawn setup refresh" })
2026-06-08T15:01:42.282942Z ERROR codex_core::tools::router: error=execution error: Io(Custom { kind: Other, error: "windows sandbox: spawn setup refresh" })
codex
Shell access is intermittently failing before PowerShell starts. I already have enough from `rg` plus the diff for a review, but I’ll try one more minimal read for exact context.
exec
"C:\\Users\\Aayushman\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "rg -n -C 4 \"No webhook secret|return true|handleWebhook|saveTweetId|catch \\(error\\)|res\\.status\\(200\\)\" src/webhookHandler.js" in C:\Repo\gitlogs
 succeeded in 434ms:
30-  }
31-  
32-  if (!secret) {
33-    console.warn('⚠️  Webhook secret not set - skipping verification (NOT RECOMMENDED)');
34:    return true;
35-  }
36-
37-  if (!signature) {
38-    console.error('❌ No signature provided in request');
--
59-  const isEnabled = database.isRepoEnabled(repoFullName);
60-  console.log(`🔍 Checking repo ${repoFullName}: isRepoEnabled=${isEnabled}`);
61-  
62-  if (isEnabled) {
63:    return true;
64-  }
65-  
66-  // Fall back to global allowed repos list (for legacy/admin use)
67-  if (!config.github.allowedRepos) {
--
111-  ];
112-  
113-  // Check message patterns
114-  if (mergePatterns.some(pattern => pattern.test(message))) {
115:    return true;
116-  }
117-  
118-  // Check if commit has multiple parents (merge commit)
119-  // Note: GitHub webhook doesn't always include parent info, so we rely on message
--
251-      throw tweetError;
252-    }
253-
254-    if (config.database.enabled) {
255:      await database.saveTweetId(repository.full_name, commit.id, tweetId);
256-    }
257-
258-    console.log(`✅ Successfully posted tweet: ${tweetId}`);
259-    console.log(`🔗 https://x.com/user/status/${tweetId}`);
260-
261-    return { success: true, tweetId };
262-
263:  } catch (error) {
264-    console.error(`❌ Error processing commit ${commit.id}:`, error.message);
265-    return { success: false, error: error.message };
266-  }
267-}
268-
269:async function handleWebhook(req, res) {
270-  try {
271-    const signature = req.headers['x-hub-signature-256'];
272-    const event = req.headers['x-github-event'];
273-    const contentType = req.headers['content-type'] || '';
--
313-    }
314-
315-    if (event !== 'push') {
316-      console.log(`ℹ️  Ignoring ${event} event`);
317:      return res.status(200).send('Event ignored');
318-    }
319-
320-    const { commits, repository, pusher, ref } = body;
321-
--
343-    console.log(`📊 Commits: ${commits.length}`);
344-
345-    if (!isRepoAllowed(repository.full_name)) {
346-      console.log(`⏭️  Repository ${repository.full_name} not in allowed list`);
347:      return res.status(200).send('Repository not allowed');
348-    }
349-
350-    // Get user associated with this repository (multi-user support)
351-    const user = database.getUserByRepo(repository.full_name);
--
392-      console.log(`📊 Queue stats: ${queueStats.currentQueueLength} pending, ${queueStats.totalProcessed} processed, ${queueStats.rateLimitRemaining} rate limit remaining`);
393-    }
394-
395-    const successCount = results.filter(r => r.success).length;
396:    res.status(200).json({
397-      status: 'OK',
398-      processed: successCount,
399-      total: nonMergeCommits.length,
400-      userId
401-    });
402-
403:  } catch (error) {
404-    console.error('❌ Webhook handler error:', error);
405:    res.status(200).send('Error processed');
406-  }
407-}
408-
409-/**
--
415-  };
416-}
417-
418-module.exports = {
419:  handleWebhook,
420-  getStats,
421-  getOrGenerateRepoContext
422-};
423-

codex
The live handler confirms the documented fail-open HMAC behavior and the top-level 200-on-error path. I’m checking whether the new tests actually cover those boundaries or only the happy-path mechanics.
exec
"C:\\Users\\Aayushman\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command 'rg -n -C 4 "async function initDatabase|function closeDatabase|saveInterval|queue_items|function saveTweetId|function getTweetsForRepo|function getUserByRepo|getRepoWebhookSecret|isRepoEnabled|storeGithubToken|storeOAuthToken|upsertUser|user_repos" src/database.js' in C:\Repo\gitlogs
exec
"C:\\Users\\Aayushman\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "rg -n -C 4 \"class QueueService|constructor|start\\(|stop\\(|restoreFromDatabase|enqueue\\(|processQueue|processItem|persistenceEnabled|setInterval|unref|requestTimestamps|deleteQueueItem|failed\" src/queueService.js" in C:\Repo\gitlogs
exec
"C:\\Users\\Aayushman\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "rg -n -C 3 \"app.use\\('/webhook/github'|express.raw|express.urlencoded|express.json|startServer|server.close|process.exit|module.exports\" src/server.js" in C:\Repo\gitlogs
exec
"C:\\Users\\Aayushman\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command 'rg -n -C 3 "pnpm install|node --check|vitest|DATABASE_PATH|WEBHOOK_SECRET|postWebhook|mockClear|deleteDatabase|unlinkSync|singleFork|GEMINI_API_KEY" .github/workflows/ci.yml .github/workflows/deploy-ec2.yml vitest.config.js tests/webhook.e2e.test.js tests/queue.test.js package.json pnpm-workspace.yaml' in C:\Repo\gitlogs
 succeeded in 699ms:
10-// File-based token storage (fallback when database is unavailable)
11-const TOKEN_FILE_PATH = path.join(process.cwd(), '.oauth_tokens.json');
12-const DB_FILE_PATH = config.database.path || './tweets.db';
13-
14:function storeOAuthTokenFile(token, userId = 'default') {
15-  try {
16-    let tokens = {};
17-    if (fs.existsSync(TOKEN_FILE_PATH)) {
18-      tokens = JSON.parse(fs.readFileSync(TOKEN_FILE_PATH, 'utf8'));
--
52-  }
53-}
54-
55-// Auto-save every 30 seconds
56:let saveInterval = null;
57-
58:async function initDatabase() {
59-  if (dbReadyPromise) return dbReadyPromise;
60-  
61-  dbReadyPromise = (async () => {
62-    try {
--
91-
92-        CREATE INDEX IF NOT EXISTS idx_users_user_id ON users(user_id);
93-
94-        -- User repositories association
95:        CREATE TABLE IF NOT EXISTS user_repos (
96-          id INTEGER PRIMARY KEY AUTOINCREMENT,
97-          user_id TEXT NOT NULL,
98-          repo_full_name TEXT NOT NULL,
99-          webhook_secret TEXT,
--
101-          created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
102-          UNIQUE(user_id, repo_full_name)
103-        );
104-
105:        CREATE INDEX IF NOT EXISTS idx_user_repos_repo ON user_repos(repo_full_name);
106:        CREATE INDEX IF NOT EXISTS idx_user_repos_user ON user_repos(user_id);
107-
108-        -- Repository context cache
109-        CREATE TABLE IF NOT EXISTS repo_contexts (
110-          id INTEGER PRIMARY KEY AUTOINCREMENT,
--
198-        CREATE INDEX IF NOT EXISTS idx_prompt_templates_user ON user_prompt_templates(user_id);
199-        CREATE INDEX IF NOT EXISTS idx_prompt_templates_active ON user_prompt_templates(user_id, is_active);
200-
201-        -- Persistent queue for surviving server restarts
202:        CREATE TABLE IF NOT EXISTS queue_items (
203-          id INTEGER PRIMARY KEY AUTOINCREMENT,
204-          queue_id TEXT UNIQUE NOT NULL,
205-          task_type TEXT NOT NULL,
206-          user_id TEXT DEFAULT 'default',
--
212-          created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
213-          updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
214-        );
215-
216:        CREATE INDEX IF NOT EXISTS idx_queue_status ON queue_items(status);
217:        CREATE INDEX IF NOT EXISTS idx_queue_priority ON queue_items(priority, created_at);
218:        CREATE INDEX IF NOT EXISTS idx_queue_user ON queue_items(user_id);
219-      `);
220-      
221-      // Additive migration: tweets tables created before the content columns
222-      // existed are upgraded in place. Driven by PRAGMA table_info (a real
--
240-      // Save initial state
241-      saveDatabase();
242-
243-      // Start auto-save interval
244:      saveInterval = setInterval(saveDatabase, 30000);
245-      
246-      dbReady = true;
247-      console.log('✅ Database initialized with sql.js (ARM64 compatible)');
248-      
--
325-  );
326-  return row ? row.tweet_id : null;
327-}
328-
329:async function saveTweetId(repoName, commitSha, tweetId) {
330-  const success = run(
331-    'INSERT OR IGNORE INTO tweets (repo_name, commit_sha, tweet_id) VALUES (?, ?, ?)',
332-    [repoName, commitSha, tweetId]
333-  );
--
336-  }
337-  return success;
338-}
339-
340:async function getTweetsForRepo(repoName) {
341-  return getAll(
342-    'SELECT * FROM tweets WHERE repo_name = ? ORDER BY created_at DESC',
343-    [repoName]
344-  );
--
428- * Store X OAuth token for a specific user
429- * @param {object} token - OAuth token object
430- * @param {string} userId - User ID (use GitHub user ID like 'github:123456' for per-user tokens)
431- */
432:function storeOAuthToken(token, userId = 'default') {
433-  if (!ensureDb()) {
434:    return storeOAuthTokenFile(token, userId);
435-  }
436-  
437-  try {
438-    run('DELETE FROM oauth_tokens WHERE user_id = ?', [userId]);
--
443-    console.log(`💾 X OAuth token stored in database for user: ${userId}`);
444-    return true;
445-  } catch (error) {
446-    console.error('❌ Error storing OAuth token:', error);
447:    return storeOAuthTokenFile(token, userId);
448-  }
449-}
450-
451-/**
--
620-// ============================================
621-// User Management Functions
622-// ============================================
623-
624:function upsertUser(userData) {
625-  if (!ensureDb()) return null;
626-  
627-  run(
628-    `INSERT INTO users (user_id, github_username, display_name, email, tier)
--
647-function getUser(userId) {
648-  return getOne('SELECT * FROM users WHERE user_id = ?', [userId]);
649-}
650-
651:function getUserByRepo(repoFullName) {
652-  return getOne(
653-    `SELECT u.* FROM users u
654:     JOIN user_repos ur ON u.user_id = ur.user_id
655-     WHERE ur.repo_full_name = ? AND ur.is_active = 1
656-     LIMIT 1`,
657-    [repoFullName]
658-  );
659-}
660-
661-function addUserRepo(userId, repoFullName, webhookSecret = null) {
662-  const success = run(
663:    `INSERT INTO user_repos (user_id, repo_full_name, webhook_secret)
664-     VALUES (?, ?, ?)
665-     ON CONFLICT(user_id, repo_full_name) DO UPDATE SET
666-       webhook_secret = COALESCE(excluded.webhook_secret, webhook_secret),
667-       is_active = 1`,
--
674-  return success;
675-}
676-
677-function getUserRepos(userId) {
678:  return getAll('SELECT * FROM user_repos WHERE user_id = ?', [userId]);
679-}
680-
681:function getRepoWebhookSecret(repoFullName) {
682-  const row = getOne(
683:    'SELECT webhook_secret FROM user_repos WHERE repo_full_name = ? AND is_active = 1 LIMIT 1',
684-    [repoFullName]
685-  );
686-  return row ? row.webhook_secret : null;
687-}
--
691-// ============================================
692-
693-function enableRepo(userId, repoFullName) {
694-  // Try to update existing
695:  run('UPDATE user_repos SET is_active = 1 WHERE user_id = ? AND repo_full_name = ?', [userId, repoFullName]);
696-  
697-  // Check if it exists, if not add it
698:  const existing = getOne('SELECT id FROM user_repos WHERE user_id = ? AND repo_full_name = ?', [userId, repoFullName]);
699-  if (!existing) {
700-    return addUserRepo(userId, repoFullName);
701-  }
702-  
--
705-}
706-
707-function disableRepo(userId, repoFullName) {
708-  const success = run(
709:    'UPDATE user_repos SET is_active = 0 WHERE user_id = ? AND repo_full_name = ?',
710-    [userId, repoFullName]
711-  );
712-  if (success) {
713-    console.log(`🚫 Disabled posting for ${repoFullName}`);
714-  }
715-  return success;
716-}
717-
718:function isRepoEnabled(repoFullName) {
719-  if (!ensureDb()) {
720-    console.log(`⚠️  Database not ready, cannot check if repo ${repoFullName} is enabled`);
721-    return false;
722-  }
723-  
724-  const row = getOne(
725:    'SELECT is_active FROM user_repos WHERE repo_full_name = ? AND is_active = 1 LIMIT 1',
726-    [repoFullName]
727-  );
728-  
729-  const isEnabled = row ? row.is_active === 1 : false;
--
733-}
734-
735-function getRepoStatus(userId, repoFullName) {
736-  const row = getOne(
737:    'SELECT is_active FROM user_repos WHERE user_id = ? AND repo_full_name = ?',
738-    [userId, repoFullName]
739-  );
740-  return row ? { enabled: row.is_active === 1 } : null;
741-}
--
786-// ============================================
787-// GitHub Token Functions (persistent storage with refresh token support)
788-// ============================================
789-
790:function storeGithubToken(githubUserId, accessToken, user, expiresAt = null, refreshToken = null) {
791-  if (!ensureDb()) {
792-    console.warn('⚠️ Database not ready, cannot store GitHub token');
793-    return false;
794-  }
--
1032-function saveQueueItem(item) {
1033-  if (!ensureDb()) return false;
1034-  
1035-  const success = run(
1036:    `INSERT INTO queue_items (queue_id, task_type, user_id, data_json, priority, status, retry_count, updated_at)
1037-     VALUES (?, ?, ?, ?, ?, ?, ?, CURRENT_TIMESTAMP)
1038-     ON CONFLICT(queue_id) DO UPDATE SET
1039-       status = excluded.status,
1040-       retry_count = excluded.retry_count,
--
1064-  if (!ensureDb()) return false;
1065-  
1066-  if (errorMessage) {
1067-    return run(
1068:      'UPDATE queue_items SET status = ?, error_message = ?, updated_at = CURRENT_TIMESTAMP WHERE queue_id = ?',
1069-      [status, errorMessage, queueId]
1070-    );
1071-  }
1072-  
1073-  return run(
1074:    'UPDATE queue_items SET status = ?, updated_at = CURRENT_TIMESTAMP WHERE queue_id = ?',
1075-    [status, queueId]
1076-  );
1077-}
1078-
--
1083-function incrementQueueItemRetry(queueId) {
1084-  if (!ensureDb()) return false;
1085-  
1086-  return run(
1087:    'UPDATE queue_items SET retry_count = retry_count + 1, status = ?, updated_at = CURRENT_TIMESTAMP WHERE queue_id = ?',
1088-    ['retrying', queueId]
1089-  );
1090-}
1091-
--
1096-function getPendingQueueItems() {
1097-  if (!ensureDb()) return [];
1098-  
1099-  const rows = getAll(
1100:    `SELECT * FROM queue_items 
1101-     WHERE status IN ('pending', 'retrying', 'processing') 
1102-     ORDER BY priority ASC, created_at ASC`
1103-  );
1104-  
--
1121- */
1122-function deleteQueueItem(queueId) {
1123-  if (!ensureDb()) return false;
1124-  
1125:  return run('DELETE FROM queue_items WHERE queue_id = ?', [queueId]);
1126-}
1127-
1128-/**
1129- * Mark processing items as pending (for restart recovery)
--
1131- */
1132-function resetProcessingQueueItems() {
1133-  if (!ensureDb()) return false;
1134-  
1135:  const count = getOne('SELECT COUNT(*) as count FROM queue_items WHERE status = ?', ['processing']);
1136-  if (count && count.count > 0) {
1137-    console.log(`🔄 Resetting ${count.count} processing queue items to pending`);
1138-    return run(
1139:      'UPDATE queue_items SET status = ?, updated_at = CURRENT_TIMESTAMP WHERE status = ?',
1140-      ['pending', 'processing']
1141-    );
1142-  }
1143-  return true;
--
1150-  if (!ensureDb()) return false;
1151-  
1152-  const oneDayAgo = new Date(Date.now() - 24 * 60 * 60 * 1000).toISOString();
1153-  return run(
1154:    'DELETE FROM queue_items WHERE status IN (?, ?) AND updated_at < ?',
1155-    ['completed', 'failed', oneDayAgo]
1156-  );
1157-}
1158-
--
1161- */
1162-function getQueueItemStats() {
1163-  if (!ensureDb()) return null;
1164-  
1165:  const pending = getOne('SELECT COUNT(*) as count FROM queue_items WHERE status = ?', ['pending']);
1166:  const processing = getOne('SELECT COUNT(*) as count FROM queue_items WHERE status = ?', ['processing']);
1167:  const retrying = getOne('SELECT COUNT(*) as count FROM queue_items WHERE status = ?', ['retrying']);
1168:  const failed = getOne('SELECT COUNT(*) as count FROM queue_items WHERE status = ?', ['failed']);
1169-  
1170-  return {
1171-    pending: pending?.count || 0,
1172-    processing: processing?.count || 0,
--
1174-    failed: failed?.count || 0
1175-  };
1176-}
1177-
1178:function closeDatabase() {
1179:  if (saveInterval) {
1180:    clearInterval(saveInterval);
1181-  }
1182-  if (db) {
1183-    saveDatabase();
1184-    db.close();
--
1203-  setOgPost,
1204-  getOgPost,
1205-  
1206-  // X OAuth functions (per-user)
1207:  storeOAuthToken,
1208-  getOAuthToken,
1209-  isOAuthTokenValid,
1210-  getRefreshToken,
1211-  deleteOAuthToken,
1212-  getXOAuthUserId,
1213-  
1214-  // User management
1215:  upsertUser,
1216-  getUser,
1217-  getUserByRepo,
1218-  addUserRepo,
1219-  getUserRepos,
1220:  getRepoWebhookSecret,
1221-  
1222-  // Repo posting control
1223-  enableRepo,
1224-  disableRepo,
1225:  isRepoEnabled,
1226-  getRepoStatus,
1227-  
1228-  // Repository context
1229-  storeRepoContext,
--
1235-  getApiUsage,
1236-  isUserOverQuota,
1237-  
1238-  // GitHub token storage
1239:  storeGithubToken,
1240-  getGithubToken,
1241-  getAllGithubTokens,
1242-  isGithubTokenValid,
1243-  deleteGithubToken,

 succeeded in 796ms:
16-const QUEUE_STATUS = {
17-  PENDING: 'pending',
18-  PROCESSING: 'processing',
19-  COMPLETED: 'completed',
20:  FAILED: 'failed',
21-  RETRYING: 'retrying'
22-};
23-
24-// Priority levels for queue items
--
41-  taskRegistry.set(taskType, taskFactory);
42-  console.log(`📝 Registered task type: ${taskType}`);
43-}
44-
45:class QueueService {
46:  constructor(options = {}) {
47-    // Queue configuration
48-    this.maxRequestsPerMinute = options.maxRequestsPerMinute || config.queue?.maxRequestsPerMinute || 15;
49-    this.maxRetries = options.maxRetries || config.queue?.maxRetries || 3;
50-    this.baseRetryDelayMs = options.baseRetryDelayMs || config.queue?.baseRetryDelayMs || 2000;
--
54-    // Queue storage (in-memory working queue, backed by database)
55-    this.queue = [];
56-    this.processing = new Map(); // Currently processing items
57-    this.completed = new Map(); // Recently completed (for deduplication)
58:    this.requestTimestamps = []; // Track request times for rate limiting
59-    
60-    // Statistics
61-    this.stats = {
62-      totalProcessed: 0,
--
75-    this.userQuotas = new Map();
76-    this.userQuotaLimit = options.userQuotaLimit || 100; // Per hour
77-    
78-    // Persistence enabled flag
79:    this.persistenceEnabled = true;
80-    
81-    console.log(`📋 Queue service initialized (rate: ${this.maxRequestsPerMinute} req/min, retries: ${this.maxRetries}, persistence: enabled)`);
82-  }
83-
84-  /**
85-   * Start the queue processor
86-   * Restores pending items from database on startup
87-   */
88:  start() {
89-    if (this.isRunning) return;
90-    
91-    this.isRunning = true;
92-    
93-    // Restore pending items from database
94:    this.restoreFromDatabase();
95-    
96-    // Clean up old items periodically (every hour)
97:    this.cleanupInterval = setInterval(() => {
98-      database.cleanupOldQueueItems();
99-    }, 60 * 60 * 1000);
100-    
101:    this.processingInterval = setInterval(() => this.processQueue(), this.processingIntervalMs);
102-    console.log('🚀 Queue processor started');
103-  }
104-
105-  /**
106-   * Stop the queue processor gracefully
107-   * Persists current state before stopping
108-   */
109:  stop() {
110-    if (!this.isRunning) return;
111-    
112-    this.isRunning = false;
113-    
--
133-  /**
134-   * Restore pending queue items from database
135-   * Called on startup to resume processing
136-   */
137:  restoreFromDatabase() {
138-    try {
139-      // Reset any items that were "processing" when server stopped
140-      database.resetProcessingQueueItems();
141-      
--
154-        // Check if we have a task factory for this task type
155-        const taskFactory = taskRegistry.get(item.taskType);
156-        
157-        if (!taskFactory) {
158:          console.warn(`⚠️  Unknown task type: ${item.taskType}, marking as failed`);
159-          database.updateQueueItemStatus(item.id, QUEUE_STATUS.FAILED, 'Unknown task type after restart');
160-          continue;
161-        }
162-        
--
191-          
192-          restored++;
193-        } catch (err) {
194-          console.error(`❌ Failed to restore queue item ${item.id}:`, err.message);
195:          database.updateQueueItemStatus(item.id, QUEUE_STATUS.FAILED, `Restore failed: ${err.message}`);
196-        }
197-      }
198-      
199-      this.stats.restoredFromDb = restored;
--
209-   * Add an item to the queue
210-   * @param {object} options - Queue item options
211-   * @returns {Promise} - Resolves when the item is processed
212-   */
213:  enqueue(options) {
214-    const {
215-      id = this.generateId(),
216-      userId,
217-      taskType, // Required for persistence - identifies how to reconstruct the task
--
257-        }
258-      };
259-
260-      // Persist to database for durability
261:      if (this.persistenceEnabled && taskType) {
262-        database.saveQueueItem({
263-          id,
264-          taskType,
265-          userId,
--
290-
291-  /**
292-   * Process items in the queue
293-   */
294:  async processQueue() {
295-    if (!this.isRunning || this.queue.length === 0) return;
296-    
297-    // Clean old request timestamps (older than 1 minute)
298-    const oneMinuteAgo = Date.now() - 60000;
299:    this.requestTimestamps = this.requestTimestamps.filter(ts => ts > oneMinuteAgo);
300-    
301-    // Check rate limit
302:    if (this.requestTimestamps.length >= this.maxRequestsPerMinute) {
303-      return; // Rate limited, wait for next cycle
304-    }
305-
306-    // Get next item to process
--
311-    item.status = QUEUE_STATUS.PROCESSING;
312-    this.stats.currentQueueLength = this.queue.length;
313-    
314-    // Update database status
315:    if (this.persistenceEnabled) {
316-      database.updateQueueItemStatus(item.id, QUEUE_STATUS.PROCESSING);
317-    }
318-
319-    const startTime = Date.now();
320-
321-    try {
322-      // Record request timestamp
323:      this.requestTimestamps.push(Date.now());
324-      
325-      // Execute the task
326-      const result = await item.task(item.data);
327-      
--
334-      this.processing.delete(item.id);
335-      this.stats.totalProcessed++;
336-      
337-      // Remove from database (completed successfully)
338:      if (this.persistenceEnabled) {
339:        database.deleteQueueItem(item.id);
340-      }
341-      
342-      console.log(`✅ Processed: ${item.id} (${processingTime}ms)`);
343-      
--
364-        item.status = QUEUE_STATUS.RETRYING;
365-        this.stats.totalRetries++;
366-        
367-        // Update database with retry status
368:        if (this.persistenceEnabled) {
369-          database.incrementQueueItemRetry(item.id);
370-        }
371-        
372-        const delay = this.calculateRetryDelay(item.retryCount, isRateLimitError);
--
389-        this.processing.delete(item.id);
390-        this.stats.totalFailed++;
391-        
392-        // Update database with failure
393:        if (this.persistenceEnabled) {
394-          database.updateQueueItemStatus(item.id, QUEUE_STATUS.FAILED, error.message);
395-        }
396-        
397-        console.log(`❌ Failed after ${this.maxRetries} retries: ${item.id} (${error.message})`);
--
494-    return {
495-      ...this.stats,
496-      currentQueueLength: this.queue.length,
497-      processingCount: this.processing.size,
498:      requestsInLastMinute: this.requestTimestamps.length,
499:      rateLimitRemaining: Math.max(0, this.maxRequestsPerMinute - this.requestTimestamps.length)
500-    };
501-  }
502-
503-  /**
--
534-
535-function getQueueService(options = {}) {
536-  if (!instance) {
537-    instance = new QueueService(options);
538:    instance.start();
539-  }
540-  return instance;
541-}
542-
--
544- * Graceful shutdown - stop queue and persist state
545- */
546-function shutdownQueueService() {
547-  if (instance) {
548:    instance.stop();
549-    console.log('📋 Queue service shutdown complete');
550-  }
551-}
552-

 succeeded in 925ms:
26-const githubStateStore = new Map();
27-
28-// Capture raw body for webhook signature verification (must be before parsing)
29:app.use('/webhook/github', express.raw({ type: '*/*' }), (req, res, next) => {
30-  req.rawBody = req.body.toString('utf8');
31-  next();
32-});
33-
34-// Parse form-encoded and JSON for webhook route
35:app.use('/webhook/github', express.urlencoded({ extended: false }));
36:app.use('/webhook/github', express.json());
37-
38-// Parse JSON for other routes
39:app.use(express.json());
40-
41-// Parse cookies
42-app.use(cookieParser());
--
1154-// binding a port or installing process-level signal handlers. The listen/shutdown
1155-// block runs only when this file is executed directly (`node src/server.js`).
1156-
1157:function startServer() {
1158-  const PORT = config.server.port;
1159-  const server = app.listen(PORT, () => {
1160-    console.log(`🚀 Git→X Bot listening on port ${PORT}`);
--
1184-    console.log(`\n🛑 Received ${signal}, starting graceful shutdown...`);
1185-
1186-    // Stop accepting new connections
1187:    server.close((err) => {
1188-      if (err) {
1189-        console.error('❌ Error closing server:', err.message);
1190-      } else {
--
1202-      database.closeDatabase();
1203-
1204-      console.log('✅ Graceful shutdown complete');
1205:      process.exit(0);
1206-    } catch (error) {
1207-      console.error('❌ Error during shutdown:', error.message);
1208:      process.exit(1);
1209-    }
1210-  }
1211-
--
1228-}
1229-
1230-if (require.main === module) {
1231:  startServer();
1232-}
1233-
1234:module.exports = app;
1235:module.exports.app = app;
1236:module.exports.startServer = startServer;
1237-

 succeeded in 992ms:
pnpm-workspace.yaml-1-# pnpm requires native build scripts to be explicitly allowlisted.
pnpm-workspace.yaml:2:# esbuild (used by vitest/vite) needs its postinstall to fetch the native binary,
pnpm-workspace.yaml-3-# otherwise the test runner fails on a clean clone (especially on Windows).
pnpm-workspace.yaml-4-allowBuilds:
pnpm-workspace.yaml-5-  esbuild: true
--
.github/workflows/ci.yml-27-          cache: pnpm
.github/workflows/ci.yml-28-
.github/workflows/ci.yml-29-      - name: Install (frozen lockfile)
.github/workflows/ci.yml:30:        run: pnpm install --frozen-lockfile
.github/workflows/ci.yml-31-
.github/workflows/ci.yml:32:      # No ESLint is configured yet; use node --check as a lightweight syntax
.github/workflows/ci.yml-33-      # gate so a broken file fails CI. (Full ESLint setup is a tracked follow-up.)
.github/workflows/ci.yml:34:      - name: Syntax check (node --check)
.github/workflows/ci.yml-35-        run: |
.github/workflows/ci.yml:36:          find src scripts -name '*.js' -print0 | xargs -0 -n1 node --check
.github/workflows/ci.yml-37-
.github/workflows/ci.yml-38-      - name: Test (Vitest)
.github/workflows/ci.yml-39-        run: pnpm test
--
tests/webhook.e2e.test.js:1:import { describe, it, expect, beforeAll, afterAll, vi } from 'vitest';
tests/webhook.e2e.test.js-2-import crypto from 'node:crypto';
tests/webhook.e2e.test.js-3-import fs from 'node:fs';
tests/webhook.e2e.test.js-4-import path from 'node:path';
--
tests/webhook.e2e.test.js-6-import { createRequire } from 'node:module';
tests/webhook.e2e.test.js-7-
tests/webhook.e2e.test.js-8-// Load the whole backend through ONE Node CommonJS require graph. This is the
tests/webhook.e2e.test.js:9:// reliable seam: vitest's vi.mock does NOT intercept transitive require()s of
tests/webhook.e2e.test.js-10-// CJS modules here, but because every module below shares the SAME require
tests/webhook.e2e.test.js-11-// cache, the stub functions we assign are the exact instances webhookHandler
tests/webhook.e2e.test.js-12-// holds by reference — so overriding them controls the real pipeline.
tests/webhook.e2e.test.js-13-//
tests/webhook.e2e.test.js:14:// Env (WEBHOOK_SECRET, ALLOWED_REPOS, ENABLE_THREADING, DATABASE_PATH) is set in
tests/webhook.e2e.test.js:15:// vitest.config.js, so it's already in process.env before these modules load.
tests/webhook.e2e.test.js-16-const require = createRequire(import.meta.url);
tests/webhook.e2e.test.js-17-const requestFactory = require('supertest');
tests/webhook.e2e.test.js-18-const request = requestFactory.default || requestFactory;
--
tests/webhook.e2e.test.js-65-  return 'sha256=' + crypto.createHmac('sha256', secret).update(rawBody).digest('hex');
tests/webhook.e2e.test.js-66-}
tests/webhook.e2e.test.js-67-
tests/webhook.e2e.test.js:68:function postWebhook({ body, signature, event = 'push' }) {
tests/webhook.e2e.test.js-69-  return request(app)
tests/webhook.e2e.test.js-70-    .post('/webhook/github')
tests/webhook.e2e.test.js-71-    .set('content-type', 'application/json')
--
tests/webhook.e2e.test.js-81-afterAll(() => {
tests/webhook.e2e.test.js-82-  shutdownQueueService();
tests/webhook.e2e.test.js-83-  database.closeDatabase();
tests/webhook.e2e.test.js:84:  const dbPath = process.env.DATABASE_PATH;
tests/webhook.e2e.test.js:85:  if (dbPath && fs.existsSync(dbPath)) fs.unlinkSync(dbPath);
tests/webhook.e2e.test.js-86-});
tests/webhook.e2e.test.js-87-
tests/webhook.e2e.test.js-88-describe('POST /webhook/github — end-to-end pipeline', () => {
tests/webhook.e2e.test.js-89-  it('verifies HMAC, runs the AI pipeline, posts to X (stub), and persists the tweet', async () => {
tests/webhook.e2e.test.js-90-    const raw = JSON.stringify(PUSH_PAYLOAD);
tests/webhook.e2e.test.js:91:    const res = await postWebhook({ body: raw, signature: sign(raw) });
tests/webhook.e2e.test.js-92-
tests/webhook.e2e.test.js-93-    expect(res.status).toBe(200);
tests/webhook.e2e.test.js-94-    expect(res.body).toMatchObject({ status: 'OK', processed: 1, total: 1 });
--
tests/webhook.e2e.test.js-110-  });
tests/webhook.e2e.test.js-111-
tests/webhook.e2e.test.js-112-  it('rejects a tampered/invalid signature with 401 and never posts', async () => {
tests/webhook.e2e.test.js:113:    twitterClient.postTweet.mockClear();
tests/webhook.e2e.test.js-114-    const raw = JSON.stringify(PUSH_PAYLOAD);
tests/webhook.e2e.test.js:115:    const res = await postWebhook({ body: raw, signature: sign(raw, 'wrong-secret') });
tests/webhook.e2e.test.js-116-
tests/webhook.e2e.test.js-117-    expect(res.status).toBe(401);
tests/webhook.e2e.test.js-118-    expect(twitterClient.postTweet).not.toHaveBeenCalled();
tests/webhook.e2e.test.js-119-  });
tests/webhook.e2e.test.js-120-
tests/webhook.e2e.test.js-121-  it('ignores non-push events (e.g. ping) with 200 and never posts', async () => {
tests/webhook.e2e.test.js:122:    twitterClient.postTweet.mockClear();
tests/webhook.e2e.test.js-123-    const raw = JSON.stringify(PUSH_PAYLOAD);
tests/webhook.e2e.test.js:124:    const res = await postWebhook({ body: raw, signature: sign(raw), event: 'ping' });
tests/webhook.e2e.test.js-125-
tests/webhook.e2e.test.js-126-    expect(res.status).toBe(200);
tests/webhook.e2e.test.js-127-    expect(twitterClient.postTweet).not.toHaveBeenCalled();
--
tests/queue.test.js:1:import { describe, it, expect, beforeEach, afterEach, vi } from 'vitest';
tests/queue.test.js-2-import os from 'node:os';
tests/queue.test.js-3-import path from 'node:path';
tests/queue.test.js-4-
tests/queue.test.js-5-// Isolated temp DB for the queue's auto-init (persistence is disabled per-instance
tests/queue.test.js-6-// below so these tests don't depend on DB writes).
tests/queue.test.js-7-process.env.NODE_ENV = 'test';
tests/queue.test.js:8:process.env.DATABASE_PATH = path.join(os.tmpdir(), `gitlogs-queue-${process.pid}.db`);
tests/queue.test.js-9-
tests/queue.test.js-10-const queueMod = await import('../src/queueService');
tests/queue.test.js-11-const QueueService = queueMod.QueueService || queueMod.default.QueueService;
--
vitest.config.js-1-import os from 'node:os';
vitest.config.js-2-import path from 'node:path';
vitest.config.js:3:import { defineConfig } from 'vitest/config';
vitest.config.js-4-
vitest.config.js-5-// A throwaway DB path for the test run. Set here (not in the test file) so it is
vitest.config.js-6-// in process.env BEFORE config/config.js + database.js read it at module load.
vitest.config.js:7:const TEST_DB = path.join(os.tmpdir(), 'gitlogs-vitest.db');
vitest.config.js-8-
vitest.config.js-9-export default defineConfig({
vitest.config.js-10-  test: {
--
vitest.config.js-15-    // The backend uses module-level singletons (sql.js DB, the queue interval).
vitest.config.js-16-    // Run in a single fork, files serially, so suites don't clobber shared state.
vitest.config.js-17-    pool: 'forks',
vitest.config.js:18:    poolOptions: { forks: { singleFork: true } },
vitest.config.js-19-    fileParallelism: false,
vitest.config.js-20-    // dotenv in config.js does NOT override already-set vars, so these win over
vitest.config.js-21-    // the committed .env — tests never touch real secrets or live services.
vitest.config.js-22-    env: {
vitest.config.js-23-      NODE_ENV: 'test',
vitest.config.js:24:      WEBHOOK_SECRET: 'test-webhook-secret-e2e',
vitest.config.js-25-      ALLOWED_REPOS: 'octo-dev/payments-api',
vitest.config.js-26-      ENABLE_THREADING: 'true',
vitest.config.js:27:      DATABASE_PATH: TEST_DB,
vitest.config.js:28:      GEMINI_API_KEY: 'test-key-not-used',
vitest.config.js-29-    },
vitest.config.js-30-  },
vitest.config.js-31-});
--
.github/workflows/deploy-ec2.yml-22-            cd ~/gitlogs
.github/workflows/deploy-ec2.yml-23-            git pull --ff-only
.github/workflows/deploy-ec2.yml-24-            # Reproducible installs from the committed lockfiles.
.github/workflows/deploy-ec2.yml:25:            pnpm install --frozen-lockfile
.github/workflows/deploy-ec2.yml-26-            pnpm --dir frontend install --frozen-lockfile
.github/workflows/deploy-ec2.yml-27-            # Build the SPA BEFORE restarting — the backend serves frontend/dist
.github/workflows/deploy-ec2.yml-28-            # (src/server.js). Without this the served frontend is stale/missing.
--
package.json-10-    "frontend:dev": "pnpm --dir frontend dev",
package.json-11-    "frontend:build": "pnpm --dir frontend build",
package.json-12-    "frontend:install": "pnpm --dir frontend install",
package.json:13:    "setup": "pnpm install && pnpm frontend:install",
package.json-14-    "build": "pnpm frontend:build",
package.json:15:    "test": "vitest run",
package.json:16:    "test:watch": "vitest",
package.json-17-    "seed": "node scripts/seed.js",
package.json-18-    "index-repo": "node src/repoIndexer.js"
package.json-19-  },
--
package.json-30-    "concurrently": "^8.2.2",
package.json-31-    "nodemon": "^3.1.9",
package.json-32-    "supertest": "^7.2.2",
package.json:33:    "vitest": "^2.1.9"
package.json-34-  },
package.json-35-  "engines": {
package.json-36-    "node": ">=18.0.0"

codex
The CI/deploy path switched package managers without showing lockfile churn in the diff. I’m checking whether the repository actually has the lockfiles those frozen installs require.
exec
"C:\\Users\\Aayushman\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command 'rg --files | rg "(''^|/)pnpm-lock'"\\.yaml"'$|(''^|/)package-lock'"\\.json"'$|(''^|/)yarn'"\\.lock"'$"' in C:\Repo\gitlogs
 succeeded in 571ms:
pnpm-lock.yaml

codex
There is no `frontend/pnpm-lock.yaml`; only a root `pnpm-lock.yaml` exists. I’m checking whether that root lockfile actually tracks the frontend importer, because the new workflow assumes frozen frontend installs are reproducible.
exec
"C:\\Users\\Aayushman\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command 'rg -n "''^importers:|''^  '"\\.:|"'^  frontend:|''^packages:|frontend/" pnpm-lock.yaml pnpm-workspace.yaml frontend/package.json package.json' in C:\Repo\gitlogs
 succeeded in 497ms:
pnpm-lock.yaml:7:importers:
pnpm-lock.yaml:9:  .:
pnpm-lock.yaml:46:packages:

codex
The root lockfile only has the root importer, not `frontend`. That makes the frontend frozen install suspect; I’ll verify the exact package-manager behavior if the local pnpm binary is available.
exec
"C:\\Users\\Aayushman\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command 'pnpm --dir frontend install --frozen-lockfile --ignore-scripts' in C:\Repo\gitlogs
2026-06-08T15:02:20.330535Z ERROR codex_core::exec: exec error: windows sandbox: spawn setup refresh
 exited -1 in 0ms:
execution error: Io(Custom { kind: Other, error: "windows sandbox: spawn setup refresh" })
2026-06-08T15:02:20.332572Z ERROR codex_core::tools::router: error=execution error: Io(Custom { kind: Other, error: "windows sandbox: spawn setup refresh" })
codex
The sandbox rejected the pnpm process launch, so I can’t execute it here. The lockfile evidence is still enough: the CI references a frontend lockfile that is not present, and the root lockfile has no `frontend` importer.
codex
The webhook test stubs external services, but I don’t see fixture setup for the actual internal DB ownership rows the handler requires. I’m checking that path so the review doesn’t accidentally accept a test that can’t exercise its advertised pipeline.
exec
"C:\\Users\\Aayushman\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "rg -n -C 10 \"getUserByRepo\\(|No user associated|xOAuthUserId|getXOAuthUserId|processCommit\\(\" src/webhookHandler.js src/database.js" in C:\Repo\gitlogs
 succeeded in 536ms:
src/database.js-606-    console.error(`❌ Failed to delete X OAuth token for user: ${userId}`);
src/database.js-607-  }
src/database.js-608-  return success;
src/database.js-609-}
src/database.js-610-
src/database.js-611-/**
src/database.js-612- * Get X OAuth user ID for a GitHub user (maps github user to their X auth)
src/database.js-613- * Returns the userId to use for X OAuth operations
src/database.js-614- * @param {string} githubUserId - GitHub user ID (without 'github:' prefix)
src/database.js-615- */
src/database.js:616:function getXOAuthUserId(githubUserId) {
src/database.js-617-  return `github:${githubUserId}`;
src/database.js-618-}
src/database.js-619-
src/database.js-620-// ============================================
src/database.js-621-// User Management Functions
src/database.js-622-// ============================================
src/database.js-623-
src/database.js-624-function upsertUser(userData) {
src/database.js-625-  if (!ensureDb()) return null;
src/database.js-626-  
--
src/database.js-641-    ]
src/database.js-642-  );
src/database.js-643-  
src/database.js-644-  return getUser(userData.userId);
src/database.js-645-}
src/database.js-646-
src/database.js-647-function getUser(userId) {
src/database.js-648-  return getOne('SELECT * FROM users WHERE user_id = ?', [userId]);
src/database.js-649-}
src/database.js-650-
src/database.js:651:function getUserByRepo(repoFullName) {
src/database.js-652-  return getOne(
src/database.js-653-    `SELECT u.* FROM users u
src/database.js-654-     JOIN user_repos ur ON u.user_id = ur.user_id
src/database.js-655-     WHERE ur.repo_full_name = ? AND ur.is_active = 1
src/database.js-656-     LIMIT 1`,
src/database.js-657-    [repoFullName]
src/database.js-658-  );
src/database.js-659-}
src/database.js-660-
src/database.js-661-function addUserRepo(userId, repoFullName, webhookSecret = null) {
--
src/database.js-1202-  // OG post functions
src/database.js-1203-  setOgPost,
src/database.js-1204-  getOgPost,
src/database.js-1205-  
src/database.js-1206-  // X OAuth functions (per-user)
src/database.js-1207-  storeOAuthToken,
src/database.js-1208-  getOAuthToken,
src/database.js-1209-  isOAuthTokenValid,
src/database.js-1210-  getRefreshToken,
src/database.js-1211-  deleteOAuthToken,
src/database.js:1212:  getXOAuthUserId,
src/database.js-1213-  
src/database.js-1214-  // User management
src/database.js-1215-  upsertUser,
src/database.js-1216-  getUser,
src/database.js-1217-  getUserByRepo,
src/database.js-1218-  addUserRepo,
src/database.js-1219-  getUserRepos,
src/database.js-1220-  getRepoWebhookSecret,
src/database.js-1221-  
src/database.js-1222-  // Repo posting control
--
src/webhookHandler.js-120-  return false;
src/webhookHandler.js-121-}
src/webhookHandler.js-122-
src/webhookHandler.js-123-/**
src/webhookHandler.js-124- * Process a single commit with enhanced context
src/webhookHandler.js-125- * Now uses two-stage AI generation for accurate changelogs
src/webhookHandler.js-126- * 
src/webhookHandler.js-127- * @param {object} commit - Git commit object
src/webhookHandler.js-128- * @param {object} repository - Repository information
src/webhookHandler.js-129- * @param {object} pusher - Pusher information
src/webhookHandler.js:130: * @param {object} options - Additional options (repoContext, userId, xOAuthUserId, githubUserId)
src/webhookHandler.js-131- */
src/webhookHandler.js:132:async function processCommit(commit, repository, pusher, options = {}) {
src/webhookHandler.js:133:  const { repoContext = null, userId = 'default', xOAuthUserId = 'default', githubUserId = null } = options;
src/webhookHandler.js-134-  
src/webhookHandler.js-135-  try {
src/webhookHandler.js:136:    console.log(`📝 Processing commit: ${commit.id.substring(0, 7)} (user: ${userId}, xAuth: ${xOAuthUserId})`);
src/webhookHandler.js-137-
src/webhookHandler.js-138-    const commitData = commitFormatter.formatCommit(commit, repository, pusher);
src/webhookHandler.js-139-
src/webhookHandler.js-140-    // Check if user is using the default template (for formatting decisions)
src/webhookHandler.js-141-    const isDefaultTemplate = templateEngine.isUsingDefaultTemplate(userId);
src/webhookHandler.js-142-    
src/webhookHandler.js-143-    // Generate changelog text (handles both template-only and AI-powered modes)
src/webhookHandler.js-144-    let changelogText = commitData.subject;
src/webhookHandler.js-145-    
src/webhookHandler.js-146-    const commitContext = {
--
src/webhookHandler.js-238-    if (config.database.enabled) {
src/webhookHandler.js-239-      quoteTweetId = await database.getOgPost(repository.full_name);
src/webhookHandler.js-240-    }
src/webhookHandler.js-241-
src/webhookHandler.js-242-    console.log('🐦 Posting to X...');
src/webhookHandler.js-243-    console.log(`📝 Tweet preview (${tweetData.length} chars): ${tweetData.substring(0, 100)}...`);
src/webhookHandler.js-244-    
src/webhookHandler.js-245-    let tweetId;
src/webhookHandler.js-246-    try {
src/webhookHandler.js-247-      // Post tweet using the user's X OAuth token
src/webhookHandler.js:248:      tweetId = await twitterClient.postTweet(tweetData, null, quoteTweetId, xOAuthUserId);
src/webhookHandler.js-249-    } catch (tweetError) {
src/webhookHandler.js-250-      console.error('❌ Twitter posting failed:', tweetError.message);
src/webhookHandler.js-251-      throw tweetError;
src/webhookHandler.js-252-    }
src/webhookHandler.js-253-
src/webhookHandler.js-254-    if (config.database.enabled) {
src/webhookHandler.js-255-      await database.saveTweetId(repository.full_name, commit.id, tweetId);
src/webhookHandler.js-256-    }
src/webhookHandler.js-257-
src/webhookHandler.js-258-    console.log(`✅ Successfully posted tweet: ${tweetId}`);
--
src/webhookHandler.js-341-    console.log(`📦 Repository: ${repository.full_name}`);
src/webhookHandler.js-342-    console.log(`🌿 Branch: ${ref || 'unknown'}`);
src/webhookHandler.js-343-    console.log(`📊 Commits: ${commits.length}`);
src/webhookHandler.js-344-
src/webhookHandler.js-345-    if (!isRepoAllowed(repository.full_name)) {
src/webhookHandler.js-346-      console.log(`⏭️  Repository ${repository.full_name} not in allowed list`);
src/webhookHandler.js-347-      return res.status(200).send('Repository not allowed');
src/webhookHandler.js-348-    }
src/webhookHandler.js-349-
src/webhookHandler.js-350-    // Get user associated with this repository (multi-user support)
src/webhookHandler.js:351:    const user = database.getUserByRepo(repository.full_name);
src/webhookHandler.js-352-    const userId = user ? user.user_id : 'default';
src/webhookHandler.js-353-    
src/webhookHandler.js-354-    // Get the X OAuth user ID for posting tweets
src/webhookHandler.js-355-    // Extract GitHub user ID from userId (e.g., 'github:123456' -> '123456')
src/webhookHandler.js:356:    let xOAuthUserId = 'default';
src/webhookHandler.js-357-    let githubUserId = null;
src/webhookHandler.js-358-    if (user && user.user_id && user.user_id.startsWith('github:')) {
src/webhookHandler.js-359-      githubUserId = user.user_id.replace('github:', '');
src/webhookHandler.js:360:      xOAuthUserId = database.getXOAuthUserId(githubUserId);
src/webhookHandler.js-361-    }
src/webhookHandler.js-362-    
src/webhookHandler.js-363-    if (user) {
src/webhookHandler.js:364:      console.log(`👤 User: ${user.display_name || user.user_id} (X auth: ${xOAuthUserId})`);
src/webhookHandler.js-365-    }
src/webhookHandler.js-366-
src/webhookHandler.js-367-    // Generate/fetch repository context for enhanced AI prompts
src/webhookHandler.js-368-    const repoContext = await getOrGenerateRepoContext(repository, commits);
src/webhookHandler.js-369-    console.log(`📋 Repo context: ${repoContext.languages?.join(', ') || 'unknown stack'}`);
src/webhookHandler.js-370-
src/webhookHandler.js-371-    // Filter out merge commits and process individual commits
src/webhookHandler.js-372-    const nonMergeCommits = commits.filter(commit => !isMergeCommit(commit));
src/webhookHandler.js-373-    
src/webhookHandler.js-374-    console.log(`📝 Processing ${nonMergeCommits.length} non-merge commits (skipped ${commits.length - nonMergeCommits.length} merge commits)`);
src/webhookHandler.js-375-
src/webhookHandler.js-376-    // Process commits with enhanced context (including two-stage diff analysis)
src/webhookHandler.js-377-    const results = [];
src/webhookHandler.js-378-    for (const commit of nonMergeCommits) {
src/webhookHandler.js:379:      const result = await processCommit(commit, repository, pusher, {
src/webhookHandler.js-380-        repoContext,
src/webhookHandler.js-381-        userId,
src/webhookHandler.js:382:        xOAuthUserId,
src/webhookHandler.js-383-        githubUserId,  // For GitHub API authentication when fetching diffs
src/webhookHandler.js-384-        ref
src/webhookHandler.js-385-      });
src/webhookHandler.js-386-      results.push(result);
src/webhookHandler.js-387-    }
src/webhookHandler.js-388-
src/webhookHandler.js-389-    // Log queue stats for monitoring
src/webhookHandler.js-390-    const queueStats = geminiClient.getQueueStats();
src/webhookHandler.js-391-    if (queueStats) {
src/webhookHandler.js-392-      console.log(`📊 Queue stats: ${queueStats.currentQueueLength} pending, ${queueStats.totalProcessed} processed, ${queueStats.rateLimitRemaining} rate limit remaining`);

codex
The webhook E2E does not create a `users`/`user_repos` ownership row; it relies on `ALLOWED_REPOS` and the legacy `default` X identity. That means it does not exercise the multi-user contract the architecture doc spends most of its time describing.
exec
"C:\\Users\\Aayushman\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "rg -n -C 5 \"initializeGemini|init|queueService = getQueueService|registerTaskType|initializeGemini\\(\\)|module\\.exports\" src/geminiClient.js" in C:\Repo\gitlogs
 succeeded in 625ms:
8- * - Tweet formatting is handled by commitFormatter
9- */
10-
11-const { GoogleGenerativeAI } = require('@google/generative-ai');
12-const config = require('../config/config');
13:const { getQueueService, registerTaskType, PRIORITY } = require('./queueService');
14-const database = require('./database');
15-const templateEngine = require('./templateEngine');
16-const diffAnalyzer = require('./diffAnalyzer');
17-
18-// Task types for queue persistence
--
38- * @param {string} prompt - The prompt to send to AI
39- * @returns {Promise<string>} - Generated text
40- */
41-async function generateAIText(prompt) {
42-  if (!model) {
43:    throw new Error('Gemini model not initialized');
44-  }
45-  
46-  const result = await model.generateContent(prompt);
47-  const response = await result.response;
48-  const text = response.text();
--
269-// ============================================
270-
271-/**
272- * Initialize Gemini AI client
273- */
274:function initGemini() {
275-  if (!config.gemini.apiKey) {
276-    console.warn('⚠️  Gemini API key not set - changelog generation disabled');
277-    return false;
278-  }
279-
--
281-    genAI = new GoogleGenerativeAI(config.gemini.apiKey);
282-    const modelName = config.gemini.model || 'gemini-pro';
283-    model = genAI.getGenerativeModel({ model: modelName });
284-    
285-    // Initialize queue service for rate limiting
286:    queueService = getQueueService({
287-      maxRequestsPerMinute: config.queue?.maxRequestsPerMinute || 15,
288-      maxRetries: config.queue?.maxRetries || 3
289-    });
290-    
291-    // Register task factories for queue persistence
292:    registerTaskType(TASK_TYPES.CHANGELOG, createChangelogTask);
293:    registerTaskType(TASK_TYPES.DETAILED_CHANGELOG, createDetailedChangelogTask);
294-    
295:    console.log(`✅ Gemini AI initialized with model: ${modelName}`);
296-    console.log(`📝 Registered ${Object.keys(TASK_TYPES).length} task types for queue persistence`);
297-    return true;
298-  } catch (error) {
299:    console.error('❌ Failed to initialize Gemini:', error.message);
300-    return false;
301-  }
302-}
303-
304-// ============================================
--
466-  if (!queueService) return -1;
467-  return queueService.getUserQuotaRemaining(userId);
468-}
469-
470-// Initialize on module load
471:const isInitialized = initGemini();
472-
473-// ============================================
474-// Exports
475-// ============================================
476-
477:module.exports = {
478-  // Core AI functions
479-  generateChangelog,
480-  generateDetailedChangelog,
481-  generateAIText,
482-  

codex
I found a documentation contradiction: the overview says all Gemini calls are queued, while the failure-mode section admits Stage 1 bypasses the queue. That is not just wording; it changes rate-limit behavior and incident expectations.
exec
"C:\\Users\\Aayushman\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command 'rg -n "All Gemini calls|Stage-1 `analyzeDiff`|HMAC fails OPEN|top-level handler|fails OPEN|GitHub API proxy|first token|sql.js durability|Live network calls|always set|always set `WEBHOOK_SECRET`" docs/ARCHITECTURE.md' in C:\Repo\gitlogs
2026-06-08T15:03:46.201740Z ERROR codex_core::exec: exec error: windows sandbox: spawn setup refresh
 exited -1 in 0ms:
execution error: Io(Custom { kind: Other, error: "windows sandbox: spawn setup refresh" })
2026-06-08T15:03:46.203139Z ERROR codex_core::tools::router: error=execution error: Io(Custom { kind: Other, error: "windows sandbox: spawn setup refresh" })
codex
The sandbox rejected that documentation search once; I’ll rely on the line positions visible in the supplied diff for the final references where needed.
codex
I’m doing one last targeted line-number pass for the architecture doc and workflow files. If the shell rejects it again, I’ll cite the exact files and the stable line numbers already returned for source/tests.
exec
"C:\\Users\\Aayushman\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command 'Select-String -Path docs/ARCHITECTURE.md,.github/workflows/ci.yml,.github/workflows/deploy-ec2.yml,pnpm-workspace.yaml,tests/webhook.e2e.test.js,tests/queue.test.js -Pattern "All Gemini calls|Stage-1|HMAC fails OPEN|top-level handler|first token|pnpm --dir frontend install|cache-dependency-path|allowBuilds|WEBHOOK_SECRET|ALLOWED_REPOS|mockClear|persistenceEnabled|restoreFromDatabase|set -euo" | ForEach-Object { "$($_.Path):$($_.LineNumber):$($_.Line)" }' in C:\Repo\gitlogs
2026-06-08T15:04:58.066214Z ERROR codex_core::exec: exec error: windows sandbox: spawn setup refresh
 exited -1 in 0ms:
execution error: Io(Custom { kind: Other, error: "windows sandbox: spawn setup refresh" })
2026-06-08T15:04:58.068161Z ERROR codex_core::tools::router: error=execution error: Io(Custom { kind: Other, error: "windows sandbox: spawn setup refresh" })
codex
That line-number pass is still blocked locally. I’ll keep the review concrete and avoid inventing exact doc line numbers beyond the source/test/workflow lines already confirmed.
codex
One more data-model issue emerged: the architecture doc models tweets as user-owned, but the live webhook write path only saves repo, SHA, and tweet ID. That matters for multi-user repos and duplicate SHAs across forks.
exec
"C:\\Users\\Aayushman\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command 'rg -n -C 4 "CREATE TABLE IF NOT EXISTS tweets|commit_sha TEXT UNIQUE|user_id TEXT|tweet_text" src/database.js' in C:\Repo\gitlogs
 succeeded in 662ms:
78-      db.run(`
79-        -- Users table for multi-user support
80-        CREATE TABLE IF NOT EXISTS users (
81-          id INTEGER PRIMARY KEY AUTOINCREMENT,
82:          user_id TEXT UNIQUE NOT NULL,
83-          github_username TEXT,
84-          display_name TEXT,
85-          email TEXT,
86-          tier TEXT DEFAULT 'free',
--
93-
94-        -- User repositories association
95-        CREATE TABLE IF NOT EXISTS user_repos (
96-          id INTEGER PRIMARY KEY AUTOINCREMENT,
97:          user_id TEXT NOT NULL,
98-          repo_full_name TEXT NOT NULL,
99-          webhook_secret TEXT,
100-          is_active INTEGER DEFAULT 1,
101-          created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
--
118-
119-        -- OAuth tokens with multi-user support
120-        CREATE TABLE IF NOT EXISTS oauth_tokens (
121-          id INTEGER PRIMARY KEY AUTOINCREMENT,
122:          user_id TEXT DEFAULT 'default',
123-          token TEXT NOT NULL,
124-          expires_at REAL NOT NULL,
125-          refresh_token TEXT,
126-          created_at DATETIME DEFAULT CURRENT_TIMESTAMP
--
129-        CREATE INDEX IF NOT EXISTS idx_oauth_expires ON oauth_tokens(expires_at DESC);
130-        CREATE INDEX IF NOT EXISTS idx_oauth_user ON oauth_tokens(user_id);
131-
132-        -- Tweets with user association
133:        CREATE TABLE IF NOT EXISTS tweets (
134-          id INTEGER PRIMARY KEY AUTOINCREMENT,
135:          user_id TEXT DEFAULT 'default',
136-          repo_name TEXT NOT NULL,
137-          commit_sha TEXT NOT NULL,
138-          tweet_id TEXT NOT NULL,
139:          tweet_text TEXT,
140-          status TEXT DEFAULT 'posted',
141-          author TEXT,
142-          created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
143-          UNIQUE(commit_sha)
--
148-
149-        -- API usage tracking for rate limiting
150-        CREATE TABLE IF NOT EXISTS api_usage (
151-          id INTEGER PRIMARY KEY AUTOINCREMENT,
152:          user_id TEXT NOT NULL DEFAULT 'default',
153-          endpoint TEXT NOT NULL,
154-          request_count INTEGER DEFAULT 1,
155-          period_start DATETIME NOT NULL,
156-          period_end DATETIME NOT NULL
--
170-
171-        -- GitHub user tokens for persistent storage with refresh token support
172-        CREATE TABLE IF NOT EXISTS github_tokens (
173-          id INTEGER PRIMARY KEY AUTOINCREMENT,
174:          github_user_id TEXT UNIQUE NOT NULL,
175-          access_token TEXT NOT NULL,
176-          refresh_token TEXT,
177-          user_json TEXT NOT NULL,
178-          expires_at DATETIME,
--
184-
185-        -- Custom prompt templates per user
186-        CREATE TABLE IF NOT EXISTS user_prompt_templates (
187-          id INTEGER PRIMARY KEY AUTOINCREMENT,
188:          user_id TEXT NOT NULL,
189-          template_id TEXT NOT NULL,
190-          template_name TEXT NOT NULL,
191-          template_content TEXT NOT NULL,
192-          is_active INTEGER DEFAULT 0,
--
202-        CREATE TABLE IF NOT EXISTS queue_items (
203-          id INTEGER PRIMARY KEY AUTOINCREMENT,
204-          queue_id TEXT UNIQUE NOT NULL,
205-          task_type TEXT NOT NULL,
206:          user_id TEXT DEFAULT 'default',
207-          data_json TEXT NOT NULL,
208-          priority INTEGER DEFAULT 2,
209-          status TEXT DEFAULT 'pending',
210-          retry_count INTEGER DEFAULT 0,
--
225-      const colStmt = db.prepare('PRAGMA table_info(tweets)');
226-      while (colStmt.step()) existingTweetCols.add(colStmt.getAsObject().name);
227-      colStmt.free();
228-      const tweetColMigrations = {
229:        tweet_text: 'TEXT',
230-        status: "TEXT DEFAULT 'posted'",
231-        author: 'TEXT',
232-      };
233-      for (const [col, decl] of Object.entries(tweetColMigrations)) {
--
374-  }
375-  try {
376-    if (createdAt) {
377-      db.run(
378:        `INSERT INTO tweets (user_id, repo_name, commit_sha, tweet_id, tweet_text, status, author, created_at)
379-         VALUES (?, ?, ?, ?, ?, ?, ?, ?)`,
380-        [userId, repoName, commitSha, tweetId, tweetText, status, author, createdAt]
381-      );
382-    } else {
383-      db.run(
384:        `INSERT INTO tweets (user_id, repo_name, commit_sha, tweet_id, tweet_text, status, author)
385-         VALUES (?, ?, ?, ?, ?, ?, ?)`,
386-        [userId, repoName, commitSha, tweetId, tweetText, status, author]
387-      );
388-    }

codex
The tweet ledger is weaker than the docs imply: `commit_sha` is globally unique and the thin save path ignores `user_id`. That creates both a multi-user correctness bug and a hidden duplicate-post/idempotency gap.
exec
"C:\\Users\\Aayushman\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command 'rg -n -C 4 "getTweetId|saveTweetId|commit_sha|already|duplicate|processCommit" src/webhookHandler.js src/database.js' in C:\Repo\gitlogs
 succeeded in 543ms:
src/database.js-133-        CREATE TABLE IF NOT EXISTS tweets (
src/database.js-134-          id INTEGER PRIMARY KEY AUTOINCREMENT,
src/database.js-135-          user_id TEXT DEFAULT 'default',
src/database.js-136-          repo_name TEXT NOT NULL,
src/database.js:137:          commit_sha TEXT NOT NULL,
src/database.js-138-          tweet_id TEXT NOT NULL,
src/database.js-139-          tweet_text TEXT,
src/database.js-140-          status TEXT DEFAULT 'posted',
src/database.js-141-          author TEXT,
src/database.js-142-          created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
src/database.js:143:          UNIQUE(commit_sha)
src/database.js-144-        );
src/database.js-145-
src/database.js-146-        CREATE INDEX IF NOT EXISTS idx_repo_created ON tweets(repo_name, created_at DESC);
src/database.js-147-        CREATE INDEX IF NOT EXISTS idx_tweets_user ON tweets(user_id);
--
src/database.js-325-  );
src/database.js-326-  return row ? row.tweet_id : null;
src/database.js-327-}
src/database.js-328-
src/database.js:329:async function saveTweetId(repoName, commitSha, tweetId) {
src/database.js-330-  const success = run(
src/database.js:331:    'INSERT OR IGNORE INTO tweets (repo_name, commit_sha, tweet_id) VALUES (?, ?, ?)',
src/database.js-332-    [repoName, commitSha, tweetId]
src/database.js-333-  );
src/database.js-334-  if (success) {
src/database.js-335-    console.log(`💾 Saved tweet ID: ${tweetId}`);
--
src/database.js-346-
src/database.js-347-/**
src/database.js-348- * Persist a FULL tweet record (user, content, status, author, timestamp).
src/database.js-349- *
src/database.js:350: * Unlike saveTweetId (the thin webhook write path that only records
src/database.js-351- * repo/sha/tweet_id and lets user_id/created_at default), this stores the
src/database.js-352- * generated tweet text and metadata so the ledger is representative — used by
src/database.js:353: * the seed/fixtures and the demo. It FAILS LOUDLY: a duplicate commit_sha or a
src/database.js-354- * not-ready DB throws rather than silently no-op'ing (no INSERT OR IGNORE).
src/database.js-355- */
src/database.js-356-async function saveTweetRecord(record) {
src/database.js-357-  if (!ensureDb()) {
--
src/database.js-374-  }
src/database.js-375-  try {
src/database.js-376-    if (createdAt) {
src/database.js-377-      db.run(
src/database.js:378:        `INSERT INTO tweets (user_id, repo_name, commit_sha, tweet_id, tweet_text, status, author, created_at)
src/database.js-379-         VALUES (?, ?, ?, ?, ?, ?, ?, ?)`,
src/database.js-380-        [userId, repoName, commitSha, tweetId, tweetText, status, author, createdAt]
src/database.js-381-      );
src/database.js-382-    } else {
src/database.js-383-      db.run(
src/database.js:384:        `INSERT INTO tweets (user_id, repo_name, commit_sha, tweet_id, tweet_text, status, author)
src/database.js-385-         VALUES (?, ?, ?, ?, ?, ?, ?)`,
src/database.js-386-        [userId, repoName, commitSha, tweetId, tweetText, status, author]
src/database.js-387-      );
src/database.js-388-    }
--
src/database.js-391-  }
src/database.js-392-  const changes = db.getRowsModified();
src/database.js-393-  if (changes !== 1) {
src/database.js-394-    throw new Error(
src/database.js:395:      `saveTweetRecord: expected exactly 1 row inserted for commit ${commitSha}, got ${changes} (duplicate commit_sha?)`
src/database.js-396-    );
src/database.js-397-  }
src/database.js-398-  saveDatabase();
src/database.js-399-  return true;
--
src/database.js-467-  if (row && row.token) {
src/database.js-468-    return JSON.parse(row.token);
src/database.js-469-  }
src/database.js-470-  
src/database.js:471:  // Fall back to 'default' for backward compatibility (only if not already querying default)
src/database.js-472-  if (userId !== 'default') {
src/database.js-473-    row = getOne(
src/database.js-474-      'SELECT token FROM oauth_tokens WHERE user_id = ? ORDER BY expires_at DESC LIMIT 1',
src/database.js-475-      ['default']
--
src/database.js-1194-  initDatabase,
src/database.js-1195-  
src/database.js-1196-  // Tweet functions
src/database.js-1197-  getLastTweetId,
src/database.js:1198:  saveTweetId,
src/database.js-1199-  saveTweetRecord,
src/database.js-1200-  getTweetsForRepo,
src/database.js-1201-  
src/database.js-1202-  // OG post functions
--
src/webhookHandler.js-128- * @param {object} repository - Repository information
src/webhookHandler.js-129- * @param {object} pusher - Pusher information
src/webhookHandler.js-130- * @param {object} options - Additional options (repoContext, userId, xOAuthUserId, githubUserId)
src/webhookHandler.js-131- */
src/webhookHandler.js:132:async function processCommit(commit, repository, pusher, options = {}) {
src/webhookHandler.js-133-  const { repoContext = null, userId = 'default', xOAuthUserId = 'default', githubUserId = null } = options;
src/webhookHandler.js-134-  
src/webhookHandler.js-135-  try {
src/webhookHandler.js-136-    console.log(`📝 Processing commit: ${commit.id.substring(0, 7)} (user: ${userId}, xAuth: ${xOAuthUserId})`);
--
src/webhookHandler.js-251-      throw tweetError;
src/webhookHandler.js-252-    }
src/webhookHandler.js-253-
src/webhookHandler.js-254-    if (config.database.enabled) {
src/webhookHandler.js:255:      await database.saveTweetId(repository.full_name, commit.id, tweetId);
src/webhookHandler.js-256-    }
src/webhookHandler.js-257-
src/webhookHandler.js-258-    console.log(`✅ Successfully posted tweet: ${tweetId}`);
src/webhookHandler.js-259-    console.log(`🔗 https://x.com/user/status/${tweetId}`);
--
src/webhookHandler.js-375-
src/webhookHandler.js-376-    // Process commits with enhanced context (including two-stage diff analysis)
src/webhookHandler.js-377-    const results = [];
src/webhookHandler.js-378-    for (const commit of nonMergeCommits) {
src/webhookHandler.js:379:      const result = await processCommit(commit, repository, pusher, {
src/webhookHandler.js-380-        repoContext,
src/webhookHandler.js-381-        userId,
src/webhookHandler.js-382-        xOAuthUserId,
src/webhookHandler.js-383-        githubUserId,  // For GitHub API authentication when fetching diffs

codex
There is also an identity fallback in token lookup: missing per-user X auth can fall back to `default`. For this product, that is not graceful degradation; it can post from the wrong account.
exec
"C:\\Users\\Aayushman\\AppData\\Local\\Microsoft\\WindowsApps\\pwsh.exe" -Command "rg -n -C 5 \"getOAuthToken\\(|fallback|default|postTweet\\(|xOAuthUserId|userId\" src/twitterClient.js src/database.js" in C:\Repo\gitlogs
 succeeded in 427ms:
src/twitterClient.js-15- * This client supports both public client (PKCE only) and confidential client modes.
src/twitterClient.js-16- * Token refresh follows OAuth 2.0 PKCE standards (RFC 7636).
src/twitterClient.js-17- */
src/twitterClient.js-18-
src/twitterClient.js-19-// Per-user X API clients (lazy initialization)
src/twitterClient.js:20:// Map of userId -> { client, authClient, initialized }
src/twitterClient.js-21-const userClients = new Map();
src/twitterClient.js-22-let oauthHandler; // OAuth handler for token management (similar to Python XAuth)
src/twitterClient.js-23-
src/twitterClient.js-24-// Initialize OAuth handler if client ID is available
src/twitterClient.js-25-if (config.twitter.clientId) {
--
src/twitterClient.js-34- * Initialize OAuth 2.0 client with PKCE support for a specific user (lazy initialization)
src/twitterClient.js-35- * 
src/twitterClient.js-36- * Uses OAuthHandler for token management (similar to Python XAuth implementation).
src/twitterClient.js-37- * Tokens are stored in database and automatically refreshed.
src/twitterClient.js-38- * 
src/twitterClient.js:39: * @param {string} userId - User ID to initialize client for (e.g., 'github:123456')
src/twitterClient.js-40- * @returns {object} - { client, authClient } for the user
src/twitterClient.js-41- */
src/twitterClient.js:42:function initializeTwitterClient(userId = 'default') {
src/twitterClient.js-43-  // Check if already initialized for this user
src/twitterClient.js:44:  const existing = userClients.get(userId);
src/twitterClient.js-45-  if (existing && existing.initialized) {
src/twitterClient.js-46-    return existing;
src/twitterClient.js-47-  }
src/twitterClient.js-48-
src/twitterClient.js-49-  if (!config.twitter.clientId) {
--
src/twitterClient.js-81-  // Get token from database for this user
src/twitterClient.js-82-  let storedToken = null;
src/twitterClient.js-83-  if (oauthHandler) {
src/twitterClient.js-84-    try {
src/twitterClient.js-85-      // Get token for specific user (supports per-user tokens)
src/twitterClient.js:86:      storedToken = require('./database').getOAuthToken(userId);
src/twitterClient.js-87-    } catch (error) {
src/twitterClient.js-88-      // Database might not have a token yet
src/twitterClient.js-89-    }
src/twitterClient.js-90-  }
src/twitterClient.js-91-
src/twitterClient.js-92-  if (!storedToken || !storedToken.access_token) {
src/twitterClient.js-93-    throw new Error(
src/twitterClient.js:94:      `No OAuth token found for user ${userId}. Please authenticate first.\n` +
src/twitterClient.js-95-      `Visit http://localhost:${config.server.port}/auth/x to authenticate with X API (OAuth 2.0 with PKCE).`
src/twitterClient.js-96-    );
src/twitterClient.js-97-  }
src/twitterClient.js-98-
src/twitterClient.js-99-  const accessToken = storedToken.access_token;
--
src/twitterClient.js-114-        `Visit http://localhost:${config.server.port}/auth/x to authenticate.`
src/twitterClient.js-115-      );
src/twitterClient.js-116-    }
src/twitterClient.js-117-
src/twitterClient.js-118-    try {
src/twitterClient.js:119:      console.log(`🔄 Refreshing token for user ${userId}...`);
src/twitterClient.js:120:      const newToken = await oauthHandler._refreshToken(userId);
src/twitterClient.js-121-      
src/twitterClient.js-122-      if (!newToken) {
src/twitterClient.js-123-        throw new Error(
src/twitterClient.js:124:          `Token refresh failed for user ${userId}. Please re-authenticate.\n` +
src/twitterClient.js-125-          `Visit http://localhost:${config.server.port}/auth/x to authenticate.`
src/twitterClient.js-126-        );
src/twitterClient.js-127-      }
src/twitterClient.js-128-
src/twitterClient.js-129-      // Update authClient with new token
--
src/twitterClient.js-135-        scope: newToken.scope,
src/twitterClient.js-136-      };
src/twitterClient.js-137-      
src/twitterClient.js-138-      return authClient.token;
src/twitterClient.js-139-    } catch (error) {
src/twitterClient.js:140:      console.error(`❌ OAuth handler refresh failed for user ${userId}:`, error.message);
src/twitterClient.js-141-      throw new Error(
src/twitterClient.js-142-        `Token refresh failed: ${error.message}\n` +
src/twitterClient.js-143-        `Please re-authenticate by visiting http://localhost:${config.server.port}/auth/x`
src/twitterClient.js-144-      );
src/twitterClient.js-145-    }
--
src/twitterClient.js-151-  const userClientData = {
src/twitterClient.js-152-    client,
src/twitterClient.js-153-    authClient,
src/twitterClient.js-154-    initialized: true
src/twitterClient.js-155-  };
src/twitterClient.js:156:  userClients.set(userId, userClientData);
src/twitterClient.js-157-  
src/twitterClient.js-158-  // Log successful initialization
src/twitterClient.js-159-  const clientType = isConfidentialClient ? 'confidential' : 'public (PKCE)';
src/twitterClient.js-160-  const hasRefreshToken = refreshToken && refreshToken !== 'dummy_refresh_token';
src/twitterClient.js:161:  console.log(`✅ X API client initialized for user ${userId} (${clientType} mode)`);
src/twitterClient.js-162-  console.log(`   Refresh token: ${hasRefreshToken ? 'Available' : 'Not available'}`);
src/twitterClient.js-163-  
src/twitterClient.js-164-  return userClientData;
src/twitterClient.js-165-}
src/twitterClient.js-166-
--
src/twitterClient.js-170- * Endpoint: https://docs.x.com/x-api/posts/manage-tweets/introduction
src/twitterClient.js-171- * 
src/twitterClient.js-172- * @param {string} text - Tweet text
src/twitterClient.js-173- * @param {Buffer} imageBuffer - Optional image buffer (not implemented)
src/twitterClient.js-174- * @param {string} quoteTweetId - Optional tweet ID to quote (OG post)
src/twitterClient.js:175: * @param {string} userId - User ID to post as (e.g., 'github:123456')
src/twitterClient.js-176- */
src/twitterClient.js:177:async function postTweet(text, imageBuffer = null, quoteTweetId = null, userId = 'default') {
src/twitterClient.js-178-  // Initialize client for this user
src/twitterClient.js:179:  const { client: twitterClient, authClient: authClientRef } = initializeTwitterClient(userId);
src/twitterClient.js-180-
src/twitterClient.js-181-  // Validate tweet text
src/twitterClient.js-182-  if (!text || typeof text !== 'string') {
src/twitterClient.js-183-    throw new Error('Tweet text is required and must be a string');
src/twitterClient.js-184-  }
--
src/twitterClient.js-210-    
src/twitterClient.js-211-    if (!response.data || !response.data.id) {
src/twitterClient.js-212-      throw new Error('Failed to get tweet ID from response');
src/twitterClient.js-213-    }
src/twitterClient.js-214-
src/twitterClient.js:215:    console.log(`✅ Tweet posted for user ${userId}: ${response.data.id}`);
src/twitterClient.js-216-    return response.data.id;
src/twitterClient.js-217-
src/twitterClient.js-218-  } catch (error) {
src/twitterClient.js-219-    // Log detailed error information
src/twitterClient.js:220:    console.error(`❌ Error posting tweet for user ${userId}:`, error);
src/twitterClient.js-221-    
src/twitterClient.js-222-    // Log the actual Twitter API error details if available
src/twitterClient.js-223-    if (error.error && error.error.errors) {
src/twitterClient.js-224-      console.error('📋 Twitter API Error Details:');
src/twitterClient.js-225-      error.error.errors.forEach((err, index) => {
--
src/twitterClient.js-247-    if (error.status === 401 || 
src/twitterClient.js-248-        (error.message && (error.message.includes('Unauthorized') || error.message.includes('expired')))) {
src/twitterClient.js-249-      // Try to refresh token and retry if we have auth client
src/twitterClient.js-250-      if (authClientRef && oauthHandler) {
src/twitterClient.js-251-        try {
src/twitterClient.js:252:          console.log(`🔄 Access token expired for user ${userId}, attempting refresh...`);
src/twitterClient.js-253-          await authClientRef.refreshAccessToken();
src/twitterClient.js-254-          console.log('✅ Token refreshed, retrying tweet post...');
src/twitterClient.js-255-          // Retry the tweet post after refresh
src/twitterClient.js-256-          const retryResponse = await twitterClient.tweets.createTweet(tweetPayload);
src/twitterClient.js-257-          if (retryResponse.data && retryResponse.data.id) {
--
src/twitterClient.js-305-
src/twitterClient.js-306-/**
src/twitterClient.js-307- * Verify credentials using X API v2 Users lookup endpoint
src/twitterClient.js-308- * Maps to: GET account/verify_credentials (v1.1) → GET /2/users/me (v2)
src/twitterClient.js-309- * Endpoint: https://docs.x.com/x-api/users/lookup/introduction
src/twitterClient.js:310: * @param {string} userId - User ID to verify credentials for
src/twitterClient.js-311- */
src/twitterClient.js:312:async function verifyCredentials(userId = 'default') {
src/twitterClient.js-313-  // Initialize client for this user
src/twitterClient.js:314:  const { client: twitterClient } = initializeTwitterClient(userId);
src/twitterClient.js-315-
src/twitterClient.js-316-  try {
src/twitterClient.js-317-    // X API v2: GET /2/users/me - Get authenticated user
src/twitterClient.js-318-    const response = await twitterClient.users.findMyUser();
src/twitterClient.js-319-    if (response.data && response.data.username) {
src/twitterClient.js:320:      console.log(`✅ X API authenticated as: @${response.data.username} (user: ${userId})`);
src/twitterClient.js-321-      return true;
src/twitterClient.js-322-    }
src/twitterClient.js-323-    return false;
src/twitterClient.js-324-  } catch (error) {
src/twitterClient.js:325:    console.error(`❌ X API authentication failed for user ${userId}:`, error.message || error.detail);
src/twitterClient.js-326-    return false;
src/twitterClient.js-327-  }
src/twitterClient.js-328-}
src/twitterClient.js-329-
src/twitterClient.js-330-/**
src/twitterClient.js-331- * Get X user info (username, profile picture, etc.)
src/twitterClient.js:332: * @param {string} userId - User ID to get X info for (e.g., 'github:123456')
src/twitterClient.js-333- */
src/twitterClient.js:334:async function getXUserInfo(userId = 'default') {
src/twitterClient.js-335-  try {
src/twitterClient.js-336-    // Initialize client for this user
src/twitterClient.js:337:    const { client: twitterClient } = initializeTwitterClient(userId);
src/twitterClient.js-338-
src/twitterClient.js-339-    // X API v2: GET /2/users/me - Get authenticated user with profile info
src/twitterClient.js:340:    // username and name are default fields, profile_image_url needs to be requested
src/twitterClient.js-341-    const response = await twitterClient.users.findMyUser({
src/twitterClient.js-342-      'user.fields': 'profile_image_url'
src/twitterClient.js-343-    });
src/twitterClient.js-344-    
src/twitterClient.js-345-    if (response.data) {
src/twitterClient.js-346-      return {
src/twitterClient.js:347:        username: response.data.username, // Always included by default
src/twitterClient.js:348:        name: response.data.name, // Always included by default
src/twitterClient.js-349-        profileImageUrl: response.data.profile_image_url || null, // Requested via user.fields
src/twitterClient.js:350:        id: response.data.id // Always included by default
src/twitterClient.js-351-      };
src/twitterClient.js-352-    }
src/twitterClient.js-353-    return null;
src/twitterClient.js-354-  } catch (error) {
src/twitterClient.js:355:    console.error(`❌ Failed to get X user info for user ${userId}:`, error.message || error.detail);
src/twitterClient.js-356-    return null;
src/twitterClient.js-357-  }
src/twitterClient.js-358-}
src/twitterClient.js-359-
src/twitterClient.js-360-module.exports = {
--
src/database.js-5-let db = null;
src/database.js-6-let SQL = null;
src/database.js-7-let dbReady = false;
src/database.js-8-let dbReadyPromise = null;
src/database.js-9-
src/database.js:10:// File-based token storage (fallback when database is unavailable)
src/database.js-11-const TOKEN_FILE_PATH = path.join(process.cwd(), '.oauth_tokens.json');
src/database.js-12-const DB_FILE_PATH = config.database.path || './tweets.db';
src/database.js-13-
src/database.js:14:function storeOAuthTokenFile(token, userId = 'default') {
src/database.js-15-  try {
src/database.js-16-    let tokens = {};
src/database.js-17-    if (fs.existsSync(TOKEN_FILE_PATH)) {
src/database.js-18-      tokens = JSON.parse(fs.readFileSync(TOKEN_FILE_PATH, 'utf8'));
src/database.js-19-    }
src/database.js:20:    tokens[userId] = token;
src/database.js-21-    fs.writeFileSync(TOKEN_FILE_PATH, JSON.stringify(tokens, null, 2), 'utf8');
src/database.js:22:    console.log(`💾 OAuth token stored in file for user: ${userId}`);
src/database.js-23-    return true;
src/database.js-24-  } catch (error) {
src/database.js-25-    console.error('❌ Error storing OAuth token to file:', error);
src/database.js-26-    return false;
src/database.js-27-  }
src/database.js-28-}
src/database.js-29-
src/database.js:30:function getOAuthTokenFile(userId = 'default') {
src/database.js-31-  try {
src/database.js-32-    if (!fs.existsSync(TOKEN_FILE_PATH)) {
src/database.js-33-      return null;
src/database.js-34-    }
src/database.js-35-    const data = JSON.parse(fs.readFileSync(TOKEN_FILE_PATH, 'utf8'));
src/database.js:36:    return data[userId] || data.default || null;
src/database.js-37-  } catch (error) {
src/database.js-38-    console.error('❌ Error reading OAuth token from file:', error);
src/database.js-39-    return null;
src/database.js-40-  }
src/database.js-41-}
--
src/database.js-117-        CREATE INDEX IF NOT EXISTS idx_repo_contexts_name ON repo_contexts(repo_full_name);
src/database.js-118-
src/database.js-119-        -- OAuth tokens with multi-user support
src/database.js-120-        CREATE TABLE IF NOT EXISTS oauth_tokens (
src/database.js-121-          id INTEGER PRIMARY KEY AUTOINCREMENT,
src/database.js:122:          user_id TEXT DEFAULT 'default',
src/database.js-123-          token TEXT NOT NULL,
src/database.js-124-          expires_at REAL NOT NULL,
src/database.js-125-          refresh_token TEXT,
src/database.js-126-          created_at DATETIME DEFAULT CURRENT_TIMESTAMP
src/database.js-127-        );
--
src/database.js-130-        CREATE INDEX IF NOT EXISTS idx_oauth_user ON oauth_tokens(user_id);
src/database.js-131-
src/database.js-132-        -- Tweets with user association
src/database.js-133-        CREATE TABLE IF NOT EXISTS tweets (
src/database.js-134-          id INTEGER PRIMARY KEY AUTOINCREMENT,
src/database.js:135:          user_id TEXT DEFAULT 'default',
src/database.js-136-          repo_name TEXT NOT NULL,
src/database.js-137-          commit_sha TEXT NOT NULL,
src/database.js-138-          tweet_id TEXT NOT NULL,
src/database.js-139-          tweet_text TEXT,
src/database.js-140-          status TEXT DEFAULT 'posted',
--
src/database.js-147-        CREATE INDEX IF NOT EXISTS idx_tweets_user ON tweets(user_id);
src/database.js-148-
src/database.js-149-        -- API usage tracking for rate limiting
src/database.js-150-        CREATE TABLE IF NOT EXISTS api_usage (
src/database.js-151-          id INTEGER PRIMARY KEY AUTOINCREMENT,
src/database.js:152:          user_id TEXT NOT NULL DEFAULT 'default',
src/database.js-153-          endpoint TEXT NOT NULL,
src/database.js-154-          request_count INTEGER DEFAULT 1,
src/database.js-155-          period_start DATETIME NOT NULL,
src/database.js-156-          period_end DATETIME NOT NULL
src/database.js-157-        );
--
src/database.js-201-        -- Persistent queue for surviving server restarts
src/database.js-202-        CREATE TABLE IF NOT EXISTS queue_items (
src/database.js-203-          id INTEGER PRIMARY KEY AUTOINCREMENT,
src/database.js-204-          queue_id TEXT UNIQUE NOT NULL,
src/database.js-205-          task_type TEXT NOT NULL,
src/database.js:206:          user_id TEXT DEFAULT 'default',
src/database.js-207-          data_json TEXT NOT NULL,
src/database.js-208-          priority INTEGER DEFAULT 2,
src/database.js-209-          status TEXT DEFAULT 'pending',
src/database.js-210-          retry_count INTEGER DEFAULT 0,
src/database.js-211-          error_message TEXT,
--
src/database.js-346-
src/database.js-347-/**
src/database.js-348- * Persist a FULL tweet record (user, content, status, author, timestamp).
src/database.js-349- *
src/database.js-350- * Unlike saveTweetId (the thin webhook write path that only records
src/database.js:351: * repo/sha/tweet_id and lets user_id/created_at default), this stores the
src/database.js-352- * generated tweet text and metadata so the ledger is representative — used by
src/database.js-353- * the seed/fixtures and the demo. It FAILS LOUDLY: a duplicate commit_sha or a
src/database.js-354- * not-ready DB throws rather than silently no-op'ing (no INSERT OR IGNORE).
src/database.js-355- */
src/database.js-356-async function saveTweetRecord(record) {
src/database.js-357-  if (!ensureDb()) {
src/database.js-358-    throw new Error('saveTweetRecord: database is not ready');
src/database.js-359-  }
src/database.js-360-  const {
src/database.js:361:    userId = 'default',
src/database.js-362-    repoName,
src/database.js-363-    commitSha,
src/database.js-364-    tweetId,
src/database.js-365-    tweetText = null,
src/database.js-366-    status = 'posted',
--
src/database.js-375-  try {
src/database.js-376-    if (createdAt) {
src/database.js-377-      db.run(
src/database.js-378-        `INSERT INTO tweets (user_id, repo_name, commit_sha, tweet_id, tweet_text, status, author, created_at)
src/database.js-379-         VALUES (?, ?, ?, ?, ?, ?, ?, ?)`,
src/database.js:380:        [userId, repoName, commitSha, tweetId, tweetText, status, author, createdAt]
src/database.js-381-      );
src/database.js-382-    } else {
src/database.js-383-      db.run(
src/database.js-384-        `INSERT INTO tweets (user_id, repo_name, commit_sha, tweet_id, tweet_text, status, author)
src/database.js-385-         VALUES (?, ?, ?, ?, ?, ?, ?)`,
src/database.js:386:        [userId, repoName, commitSha, tweetId, tweetText, status, author]
src/database.js-387-      );
src/database.js-388-    }
src/database.js-389-  } catch (error) {
src/database.js-390-    throw new Error(`saveTweetRecord: insert failed for commit ${commitSha}: ${error.message}`);
src/database.js-391-  }
--
src/database.js-425-// ============================================
src/database.js-426-
src/database.js-427-/**
src/database.js-428- * Store X OAuth token for a specific user
src/database.js-429- * @param {object} token - OAuth token object
src/database.js:430: * @param {string} userId - User ID (use GitHub user ID like 'github:123456' for per-user tokens)
src/database.js-431- */
src/database.js:432:function storeOAuthToken(token, userId = 'default') {
src/database.js-433-  if (!ensureDb()) {
src/database.js:434:    return storeOAuthTokenFile(token, userId);
src/database.js-435-  }
src/database.js-436-  
src/database.js-437-  try {
src/database.js:438:    run('DELETE FROM oauth_tokens WHERE user_id = ?', [userId]);
src/database.js-439-    run(
src/database.js-440-      'INSERT INTO oauth_tokens (user_id, token, expires_at, refresh_token) VALUES (?, ?, ?, ?)',
src/database.js:441:      [userId, JSON.stringify(token), token.expires_at || 0, token.refresh_token || null]
src/database.js-442-    );
src/database.js:443:    console.log(`💾 X OAuth token stored in database for user: ${userId}`);
src/database.js-444-    return true;
src/database.js-445-  } catch (error) {
src/database.js-446-    console.error('❌ Error storing OAuth token:', error);
src/database.js:447:    return storeOAuthTokenFile(token, userId);
src/database.js-448-  }
src/database.js-449-}
src/database.js-450-
src/database.js-451-/**
src/database.js-452- * Get X OAuth token for a specific user
src/database.js:453: * Falls back to 'default' if user-specific token not found (for backward compatibility)
src/database.js:454: * @param {string} userId - User ID to get token for
src/database.js-455- */
src/database.js:456:function getOAuthToken(userId = 'default') {
src/database.js-457-  if (!ensureDb()) {
src/database.js:458:    return getOAuthTokenFile(userId);
src/database.js-459-  }
src/database.js-460-  
src/database.js-461-  // Try user-specific token first
src/database.js-462-  let row = getOne(
src/database.js-463-    'SELECT token FROM oauth_tokens WHERE user_id = ? ORDER BY expires_at DESC LIMIT 1',
src/database.js:464:    [userId]
src/database.js-465-  );
src/database.js-466-  
src/database.js-467-  if (row && row.token) {
src/database.js-468-    return JSON.parse(row.token);
src/database.js-469-  }
src/database.js-470-  
src/database.js:471:  // Fall back to 'default' for backward compatibility (only if not already querying default)
src/database.js:472:  if (userId !== 'default') {
src/database.js-473-    row = getOne(
src/database.js-474-      'SELECT token FROM oauth_tokens WHERE user_id = ? ORDER BY expires_at DESC LIMIT 1',
src/database.js:475:      ['default']
src/database.js-476-    );
src/database.js-477-    if (row && row.token) {
src/database.js:478:      console.log(`⚠️  Using legacy 'default' X OAuth token for user: ${userId}`);
src/database.js-479-      return JSON.parse(row.token);
src/database.js-480-    }
src/database.js-481-  }
src/database.js-482-  
src/database.js:483:  return getOAuthTokenFile(userId);
src/database.js-484-}
src/database.js-485-
src/database.js-486-/**
src/database.js:487: * Get X OAuth token for a specific user WITHOUT fallback to 'default'
src/database.js-488- * Use this when checking connection status to avoid false positives
src/database.js:489: * @param {string} userId - User ID to get token for
src/database.js-490- */
src/database.js:491:function getOAuthTokenNoFallback(userId) {
src/database.js-492-  if (!ensureDb()) {
src/database.js:493:    // Check file storage without fallback
src/database.js-494-    try {
src/database.js-495-      if (!fs.existsSync(TOKEN_FILE_PATH)) {
src/database.js-496-        return null;
src/database.js-497-      }
src/database.js-498-      const data = JSON.parse(fs.readFileSync(TOKEN_FILE_PATH, 'utf8'));
src/database.js:499:      return data[userId] || null; // No fallback to data.default
src/database.js-500-    } catch (error) {
src/database.js-501-      return null;
src/database.js-502-    }
src/database.js-503-  }
src/database.js-504-  
src/database.js:505:  // Try user-specific token only, no fallback
src/database.js-506-  const row = getOne(
src/database.js-507-    'SELECT token FROM oauth_tokens WHERE user_id = ? ORDER BY expires_at DESC LIMIT 1',
src/database.js:508:    [userId]
src/database.js-509-  );
src/database.js-510-  
src/database.js-511-  if (row && row.token) {
src/database.js-512-    return JSON.parse(row.token);
src/database.js-513-  }
--
src/database.js-515-  return null;
src/database.js-516-}
src/database.js-517-
src/database.js-518-/**
src/database.js-519- * Check if X OAuth token is valid for a specific user
src/database.js:520: * Does NOT fallback to 'default' token - checks only user-specific token
src/database.js:521: * @param {string} userId - User ID to check
src/database.js-522- */
src/database.js:523:function isOAuthTokenValid(userId = 'default') {
src/database.js:524:  // Use no-fallback version to check if THIS user specifically has a valid token
src/database.js:525:  const token = getOAuthTokenNoFallback(userId);
src/database.js-526-  if (!token) return false;
src/database.js-527-  const expiresAt = token.expires_at || 0;
src/database.js-528-  return Date.now() / 1000 < expiresAt;
src/database.js-529-}
src/database.js-530-
src/database.js-531-/**
src/database.js-532- * Get X OAuth refresh token for a specific user
src/database.js:533: * @param {string} userId - User ID to get refresh token for
src/database.js-534- */
src/database.js:535:function getRefreshToken(userId = 'default') {
src/database.js-536-  if (!ensureDb()) {
src/database.js:537:    const token = getOAuthTokenFile(userId);
src/database.js-538-    return token ? token.refresh_token : null;
src/database.js-539-  }
src/database.js-540-  
src/database.js-541-  // Try user-specific token first
src/database.js-542-  let row = getOne(
src/database.js-543-    'SELECT refresh_token FROM oauth_tokens WHERE user_id = ? ORDER BY expires_at DESC LIMIT 1',
src/database.js:544:    [userId]
src/database.js-545-  );
src/database.js-546-  
src/database.js-547-  if (row && row.refresh_token) {
src/database.js-548-    return row.refresh_token;
src/database.js-549-  }
src/database.js-550-  
src/database.js:551:  // Fall back to 'default' for backward compatibility
src/database.js:552:  if (userId !== 'default') {
src/database.js-553-    row = getOne(
src/database.js-554-      'SELECT refresh_token FROM oauth_tokens WHERE user_id = ? ORDER BY expires_at DESC LIMIT 1',
src/database.js:555:      ['default']
src/database.js-556-    );
src/database.js-557-    if (row && row.refresh_token) {
src/database.js-558-      return row.refresh_token;
src/database.js-559-    }
src/database.js-560-  }
src/database.js-561-  
src/database.js:562:  const token = getOAuthTokenFile(userId);
src/database.js-563-  return token ? token.refresh_token : null;
src/database.js-564-}
src/database.js-565-
src/database.js-566-/**
src/database.js-567- * Delete X OAuth token for a specific user
src/database.js-568- * Deletes from both database and file storage
src/database.js:569: * @param {string} userId - User ID to delete token for
src/database.js-570- */
src/database.js:571:function deleteOAuthToken(userId) {
src/database.js-572-  let dbSuccess = false;
src/database.js-573-  let fileSuccess = false;
src/database.js-574-  
src/database.js-575-  // Delete from database if available
src/database.js-576-  if (ensureDb()) {
src/database.js:577:    dbSuccess = run('DELETE FROM oauth_tokens WHERE user_id = ?', [userId]);
src/database.js-578-    if (dbSuccess) {
src/database.js:579:      console.log(`🗑️ X OAuth token deleted from database for user: ${userId}`);
src/database.js-580-    }
src/database.js-581-  }
src/database.js-582-  
src/database.js-583-  // Also delete from file storage
src/database.js-584-  try {
src/database.js-585-    if (fs.existsSync(TOKEN_FILE_PATH)) {
src/database.js-586-      const tokens = JSON.parse(fs.readFileSync(TOKEN_FILE_PATH, 'utf8'));
src/database.js:587:      if (tokens[userId]) {
src/database.js:588:        delete tokens[userId];
src/database.js-589-        fs.writeFileSync(TOKEN_FILE_PATH, JSON.stringify(tokens, null, 2), 'utf8');
src/database.js-590-        fileSuccess = true;
src/database.js:591:        console.log(`🗑️ X OAuth token deleted from file for user: ${userId}`);
src/database.js-592-      } else {
src/database.js-593-        fileSuccess = true; // Token not in file, consider it successful
src/database.js-594-      }
src/database.js-595-    } else {
src/database.js-596-      fileSuccess = true; // No file to delete from, consider it successful
--
src/database.js-599-    console.error('❌ Error deleting OAuth token from file:', error);
src/database.js-600-  }
src/database.js-601-  
src/database.js-602-  const success = dbSuccess || fileSuccess;
src/database.js-603-  if (success) {
src/database.js:604:    console.log(`✅ X OAuth token deleted successfully for user: ${userId}`);
src/database.js-605-  } else {
src/database.js:606:    console.error(`❌ Failed to delete X OAuth token for user: ${userId}`);
src/database.js-607-  }
src/database.js-608-  return success;
src/database.js-609-}
src/database.js-610-
src/database.js-611-/**
src/database.js-612- * Get X OAuth user ID for a GitHub user (maps github user to their X auth)
src/database.js:613: * Returns the userId to use for X OAuth operations
src/database.js-614- * @param {string} githubUserId - GitHub user ID (without 'github:' prefix)
src/database.js-615- */
src/database.js-616-function getXOAuthUserId(githubUserId) {
src/database.js-617-  return `github:${githubUserId}`;
src/database.js-618-}
--
src/database.js-631-       github_username = excluded.github_username,
src/database.js-632-       display_name = excluded.display_name,
src/database.js-633-       email = excluded.email,
src/database.js-634-       updated_at = CURRENT_TIMESTAMP`,
src/database.js-635-    [
src/database.js:636:      userData.userId,
src/database.js-637-      userData.githubUsername || null,
src/database.js-638-      userData.displayName || null,
src/database.js-639-      userData.email || null,
src/database.js-640-      userData.tier || 'free'
src/database.js-641-    ]
src/database.js-642-  );
src/database.js-643-  
src/database.js:644:  return getUser(userData.userId);
src/database.js-645-}
src/database.js-646-
src/database.js:647:function getUser(userId) {
src/database.js:648:  return getOne('SELECT * FROM users WHERE user_id = ?', [userId]);
src/database.js-649-}
src/database.js-650-
src/database.js-651-function getUserByRepo(repoFullName) {
src/database.js-652-  return getOne(
src/database.js-653-    `SELECT u.* FROM users u
--
src/database.js-656-     LIMIT 1`,
src/database.js-657-    [repoFullName]
src/database.js-658-  );
src/database.js-659-}
src/database.js-660-
src/database.js:661:function addUserRepo(userId, repoFullName, webhookSecret = null) {
src/database.js-662-  const success = run(
src/database.js-663-    `INSERT INTO user_repos (user_id, repo_full_name, webhook_secret)
src/database.js-664-     VALUES (?, ?, ?)
src/database.js-665-     ON CONFLICT(user_id, repo_full_name) DO UPDATE SET
src/database.js-666-       webhook_secret = COALESCE(excluded.webhook_secret, webhook_secret),
src/database.js-667-       is_active = 1`,
src/database.js:668:    [userId, repoFullName, webhookSecret]
src/database.js-669-  );
src/database.js-670-  
src/database.js-671-  if (success) {
src/database.js:672:    console.log(`📁 Repo ${repoFullName} associated with user ${userId}`);
src/database.js-673-  }
src/database.js-674-  return success;
src/database.js-675-}
src/database.js-676-
src/database.js:677:function getUserRepos(userId) {
src/database.js:678:  return getAll('SELECT * FROM user_repos WHERE user_id = ?', [userId]);
src/database.js-679-}
src/database.js-680-
src/database.js-681-function getRepoWebhookSecret(repoFullName) {
src/database.js-682-  const row = getOne(
src/database.js-683-    'SELECT webhook_secret FROM user_repos WHERE repo_full_name = ? AND is_active = 1 LIMIT 1',
--
src/database.js-688-
src/database.js-689-// ============================================
src/database.js-690-// Repo Posting Control Functions
src/database.js-691-// ============================================
src/database.js-692-
src/database.js:693:function enableRepo(userId, repoFullName) {
src/database.js-694-  // Try to update existing
src/database.js:695:  run('UPDATE user_repos SET is_active = 1 WHERE user_id = ? AND repo_full_name = ?', [userId, repoFullName]);
src/database.js-696-  
src/database.js-697-  // Check if it exists, if not add it
src/database.js:698:  const existing = getOne('SELECT id FROM user_repos WHERE user_id = ? AND repo_full_name = ?', [userId, repoFullName]);
src/database.js-699-  if (!existing) {
src/database.js:700:    return addUserRepo(userId, repoFullName);
src/database.js-701-  }
src/database.js-702-  
src/database.js-703-  console.log(`✅ Enabled posting for ${repoFullName}`);
src/database.js-704-  return true;
src/database.js-705-}
src/database.js-706-
src/database.js:707:function disableRepo(userId, repoFullName) {
src/database.js-708-  const success = run(
src/database.js-709-    'UPDATE user_repos SET is_active = 0 WHERE user_id = ? AND repo_full_name = ?',
src/database.js:710:    [userId, repoFullName]
src/database.js-711-  );
src/database.js-712-  if (success) {
src/database.js-713-    console.log(`🚫 Disabled posting for ${repoFullName}`);
src/database.js-714-  }
src/database.js-715-  return success;
--
src/database.js-730-  console.log(`🗄️  DB check for ${repoFullName}: row=${JSON.stringify(row)}, isEnabled=${isEnabled}`);
src/database.js-731-  
src/database.js-732-  return isEnabled;
src/database.js-733-}
src/database.js-734-
src/database.js:735:function getRepoStatus(userId, repoFullName) {
src/database.js-736-  const row = getOne(
src/database.js-737-    'SELECT is_active FROM user_repos WHERE user_id = ? AND repo_full_name = ?',
src/database.js:738:    [userId, repoFullName]
src/database.js-739-  );
src/database.js-740-  return row ? { enabled: row.is_active === 1 } : null;
src/database.js-741-}
src/database.js-742-
src/database.js-743-// ============================================
--
src/database.js-896-
src/database.js-897-// ============================================
src/database.js-898-// API Usage Tracking Functions
src/database.js-899-// ============================================
src/database.js-900-
src/database.js:901:function trackApiUsage(userId, endpoint = 'gemini') {
src/database.js-902-  if (!ensureDb()) return;
src/database.js-903-  
src/database.js-904-  const now = new Date();
src/database.js-905-  const periodStart = new Date(now.getFullYear(), now.getMonth(), now.getDate(), now.getHours());
src/database.js-906-  const periodEnd = new Date(periodStart.getTime() + 3600000);
src/database.js-907-  
src/database.js-908-  const existing = getOne(
src/database.js-909-    'SELECT id, request_count FROM api_usage WHERE user_id = ? AND endpoint = ? AND period_start = ?',
src/database.js:910:    [userId, endpoint, periodStart.toISOString()]
src/database.js-911-  );
src/database.js-912-  
src/database.js-913-  if (existing) {
src/database.js-914-    run('UPDATE api_usage SET request_count = request_count + 1 WHERE id = ?', [existing.id]);
src/database.js-915-  } else {
src/database.js-916-    run(
src/database.js-917-      'INSERT INTO api_usage (user_id, endpoint, request_count, period_start, period_end) VALUES (?, ?, 1, ?, ?)',
src/database.js:918:      [userId, endpoint, periodStart.toISOString(), periodEnd.toISOString()]
src/database.js-919-    );
src/database.js-920-  }
src/database.js-921-}
src/database.js-922-
src/database.js:923:function getApiUsage(userId, endpoint = 'gemini') {
src/database.js-924-  const now = new Date();
src/database.js-925-  const periodStart = new Date(now.getFullYear(), now.getMonth(), now.getDate(), now.getHours());
src/database.js-926-  
src/database.js-927-  const row = getOne(
src/database.js-928-    'SELECT request_count FROM api_usage WHERE user_id = ? AND endpoint = ? AND period_start = ?',
src/database.js:929:    [userId, endpoint, periodStart.toISOString()]
src/database.js-930-  );
src/database.js-931-  
src/database.js-932-  return row ? row.request_count : 0;
src/database.js-933-}
src/database.js-934-
src/database.js:935:function isUserOverQuota(userId, endpoint = 'gemini') {
src/database.js:936:  const user = getUser(userId);
src/database.js-937-  const limit = user ? user.api_quota_limit : 100;
src/database.js:938:  const usage = getApiUsage(userId, endpoint);
src/database.js-939-  return usage >= limit;
src/database.js-940-}
src/database.js-941-
src/database.js-942-// ============================================
src/database.js-943-// Prompt Template Functions
src/database.js-944-// ============================================
src/database.js-945-
src/database.js:946:function savePromptTemplate(userId, templateId, templateName, templateContent) {
src/database.js-947-  if (!ensureDb()) return false;
src/database.js-948-  
src/database.js-949-  const success = run(
src/database.js-950-    `INSERT INTO user_prompt_templates (user_id, template_id, template_name, template_content, updated_at)
src/database.js-951-     VALUES (?, ?, ?, ?, CURRENT_TIMESTAMP)
src/database.js-952-     ON CONFLICT(user_id, template_id) DO UPDATE SET
src/database.js-953-       template_name = excluded.template_name,
src/database.js-954-       template_content = excluded.template_content,
src/database.js-955-       updated_at = CURRENT_TIMESTAMP`,
src/database.js:956:    [userId, templateId, templateName, templateContent]
src/database.js-957-  );
src/database.js-958-  
src/database.js-959-  if (success) {
src/database.js:960:    console.log(`📝 Prompt template saved for user ${userId}: ${templateId}`);
src/database.js-961-  }
src/database.js-962-  return success;
src/database.js-963-}
src/database.js-964-
src/database.js:965:function getPromptTemplates(userId) {
src/database.js-966-  if (!ensureDb()) return [];
src/database.js:967:  return getAll('SELECT * FROM user_prompt_templates WHERE user_id = ? ORDER BY created_at DESC', [userId]);
src/database.js-968-}
src/database.js-969-
src/database.js:970:function getActivePromptTemplate(userId) {
src/database.js-971-  if (!ensureDb()) return null;
src/database.js:972:  return getOne('SELECT * FROM user_prompt_templates WHERE user_id = ? AND is_active = 1', [userId]);
src/database.js-973-}
src/database.js-974-
src/database.js:975:function setActivePromptTemplate(userId, templateId) {
src/database.js-976-  if (!ensureDb()) return false;
src/database.js-977-  
src/database.js-978-  // First, deactivate all templates for this user
src/database.js:979:  run('UPDATE user_prompt_templates SET is_active = 0 WHERE user_id = ?', [userId]);
src/database.js-980-  
src/database.js:981:  // If templateId is null or 'default', just deactivate (use default)
src/database.js:982:  if (!templateId || templateId === 'default') {
src/database.js:983:    console.log(`📝 Reset to default prompt template for user ${userId}`);
src/database.js-984-    return true;
src/database.js-985-  }
src/database.js-986-  
src/database.js-987-  // Check if template exists before trying to activate
src/database.js-988-  const existing = getOne(
src/database.js-989-    'SELECT id FROM user_prompt_templates WHERE user_id = ? AND template_id = ?',
src/database.js:990:    [userId, templateId]
src/database.js-991-  );
src/database.js-992-  
src/database.js-993-  if (!existing) {
src/database.js:994:    console.warn(`⚠️  Template ${templateId} not found for user ${userId}, cannot activate`);
src/database.js-995-    return false;
src/database.js-996-  }
src/database.js-997-  
src/database.js-998-  // Activate the specified template
src/database.js-999-  const success = run(
src/database.js-1000-    'UPDATE user_prompt_templates SET is_active = 1 WHERE user_id = ? AND template_id = ?',
src/database.js:1001:    [userId, templateId]
src/database.js-1002-  );
src/database.js-1003-  
src/database.js-1004-  if (success) {
src/database.js:1005:    console.log(`📝 Activated prompt template ${templateId} for user ${userId}`);
src/database.js-1006-  }
src/database.js-1007-  return success;
src/database.js-1008-}
src/database.js-1009-
src/database.js:1010:function deletePromptTemplate(userId, templateId) {
src/database.js-1011-  if (!ensureDb()) return false;
src/database.js-1012-  
src/database.js-1013-  const success = run(
src/database.js-1014-    'DELETE FROM user_prompt_templates WHERE user_id = ? AND template_id = ?',
src/database.js:1015:    [userId, templateId]
src/database.js-1016-  );
src/database.js-1017-  
src/database.js-1018-  if (success) {
src/database.js:1019:    console.log(`🗑️ Deleted prompt template ${templateId} for user ${userId}`);
src/database.js-1020-  }
src/database.js-1021-  return success;
src/database.js-1022-}
src/database.js-1023-
src/database.js-1024-// ============================================
--
src/database.js-1041-       error_message = excluded.error_message,
src/database.js-1042-       updated_at = CURRENT_TIMESTAMP`,
src/database.js-1043-    [
src/database.js-1044-      item.id,
src/database.js-1045-      item.taskType,
src/database.js:1046:      item.userId || 'default',
src/database.js-1047-      JSON.stringify(item.data),
src/database.js-1048-      item.priority || 2,
src/database.js-1049-      item.status || 'pending',
src/database.js-1050-      item.retryCount || 0
src/database.js-1051-    ]
--
src/database.js-1103-  );
src/database.js-1104-  
src/database.js-1105-  return rows.map(row => ({
src/database.js-1106-    id: row.queue_id,
src/database.js-1107-    taskType: row.task_type,
src/database.js:1108:    userId: row.user_id,
src/database.js-1109-    data: JSON.parse(row.data_json),
src/database.js-1110-    priority: row.priority,
src/database.js-1111-    status: row.status,
src/database.js-1112-    retryCount: row.retry_count,
src/database.js-1113-    createdAt: new Date(row.created_at).getTime(),

codex
**Findings**

1. **Critical: frontend CI/deploy is built on a missing lockfile.**  
   `.github/workflows/ci.yml:55` points at `frontend/pnpm-lock.yaml`, and `.github/workflows/ci.yml:58` plus `.github/workflows/deploy-ec2.yml:26` run frozen frontend installs. The repo only has root `pnpm-lock.yaml`, and it only has the root importer. `pnpm-workspace.yaml:1` also does not declare `packages`, so the frontend is not a tracked workspace package. This CI/deploy path is either going to fail or silently drift from what you think is locked.

2. **Critical: multi-user X identity is unsafe.**  
   `src/database.js:471-479` falls back from a missing user token to the legacy `default` token, and `src/twitterClient.js:86` uses that lookup before posting. That means a user without a valid X token can post from the wrong account. The new architecture doc claims per-user X ownership, while the live behavior violates it.

3. **Critical: webhook auth still fails open.**  
   `src/webhookHandler.js:32-34` returns `true` when no webhook secret exists. The architecture doc calls this out but the patch does not fix or gate it. Anyone who can reach `/webhook/github` can forge push events if config is wrong. Documentation is not a control.

4. **High: webhook failures are acknowledged as success and work is lost.**  
   `src/webhookHandler.js:263-265` swallows per-commit failures, and `src/webhookHandler.js:403-405` returns `200` even on unexpected handler errors. GitHub will not retry. This is silent data loss dressed up as resilience.

5. **High: the webhook E2E does not test the actual multi-user contract.**  
   `tests/webhook.e2e.test.js:24-28` uses `ALLOWED_REPOS`, but the test never seeds `users`, `user_repos`, per-repo webhook secret, or per-user X auth. The live handler therefore falls back through `src/webhookHandler.js:351-360` to `default`. This test proves the legacy path, not the product path described in `docs/ARCHITECTURE.md`.

6. **High: tweet persistence is not user-scoped and is not idempotent.**  
   `src/database.js:132-143` makes `commit_sha` globally unique, while `src/database.js:329-333` inserts only `repo_name`, `commit_sha`, and `tweet_id`. `src/webhookHandler.js:255` uses that thin path after posting. Duplicate webhook delivery can post a second tweet, then `INSERT OR IGNORE` hides the duplicate ledger write. Forks or multi-user repos can also collide on SHA.

7. **High: queue persistence is claimed, not tested.**  
   `tests/queue.test.js:21` disables persistence, so the tests do not cover restart restore, DB status transitions, retry persistence, unknown task recovery, or failed-item behavior. Worse, `QueueService.start()` still calls `restoreFromDatabase()` unconditionally at `src/queueService.js:94`, so the “pure in-memory” test setup is leaky.

8. **Medium: architecture doc contradicts the implementation.**  
   `docs/ARCHITECTURE.md` says all Gemini calls flow through the queue, but `src/geminiClient.js:115` runs Stage 1 directly and only Stage 2 queues around `src/geminiClient.js:359`. That is an operationally important lie: Stage 1 can still hit rate limits outside the queue.

9. **Medium: deploy runs dependency install/build on the production host.**  
   `.github/workflows/deploy-ec2.yml:25-30` installs dependencies and builds the SPA on EC2. That executes package lifecycle scripts on the server and requires dev/build tooling in prod. Build in CI, deploy an artifact, then restart.

10. **Medium: CI token permissions are not locked down.**  
   `.github/workflows/ci.yml` has no top-level `permissions: contents: read`. Do not leave default `GITHUB_TOKEN` scope implicit, especially on `pull_request`.

I did not run the suite; the sandbox rejected the `pnpm` process launch. The static evidence is already enough to block this diff.
tokens used
111,240
**Findings**

1. **Critical: frontend CI/deploy is built on a missing lockfile.**  
   `.github/workflows/ci.yml:55` points at `frontend/pnpm-lock.yaml`, and `.github/workflows/ci.yml:58` plus `.github/workflows/deploy-ec2.yml:26` run frozen frontend installs. The repo only has root `pnpm-lock.yaml`, and it only has the root importer. `pnpm-workspace.yaml:1` also does not declare `packages`, so the frontend is not a tracked workspace package. This CI/deploy path is either going to fail or silently drift from what you think is locked.

2. **Critical: multi-user X identity is unsafe.**  
   `src/database.js:471-479` falls back from a missing user token to the legacy `default` token, and `src/twitterClient.js:86` uses that lookup before posting. That means a user without a valid X token can post from the wrong account. The new architecture doc claims per-user X ownership, while the live behavior violates it.

3. **Critical: webhook auth still fails open.**  
   `src/webhookHandler.js:32-34` returns `true` when no webhook secret exists. The architecture doc calls this out but the patch does not fix or gate it. Anyone who can reach `/webhook/github` can forge push events if config is wrong. Documentation is not a control.

4. **High: webhook failures are acknowledged as success and work is lost.**  
   `src/webhookHandler.js:263-265` swallows per-commit failures, and `src/webhookHandler.js:403-405` returns `200` even on unexpected handler errors. GitHub will not retry. This is silent data loss dressed up as resilience.

5. **High: the webhook E2E does not test the actual multi-user contract.**  
   `tests/webhook.e2e.test.js:24-28` uses `ALLOWED_REPOS`, but the test never seeds `users`, `user_repos`, per-repo webhook secret, or per-user X auth. The live handler therefore falls back through `src/webhookHandler.js:351-360` to `default`. This test proves the legacy path, not the product path described in `docs/ARCHITECTURE.md`.

6. **High: tweet persistence is not user-scoped and is not idempotent.**  
   `src/database.js:132-143` makes `commit_sha` globally unique, while `src/database.js:329-333` inserts only `repo_name`, `commit_sha`, and `tweet_id`. `src/webhookHandler.js:255` uses that thin path after posting. Duplicate webhook delivery can post a second tweet, then `INSERT OR IGNORE` hides the duplicate ledger write. Forks or multi-user repos can also collide on SHA.

7. **High: queue persistence is claimed, not tested.**  
   `tests/queue.test.js:21` disables persistence, so the tests do not cover restart restore, DB status transitions, retry persistence, unknown task recovery, or failed-item behavior. Worse, `QueueService.start()` still calls `restoreFromDatabase()` unconditionally at `src/queueService.js:94`, so the “pure in-memory” test setup is leaky.

8. **Medium: architecture doc contradicts the implementation.**  
   `docs/ARCHITECTURE.md` says all Gemini calls flow through the queue, but `src/geminiClient.js:115` runs Stage 1 directly and only Stage 2 queues around `src/geminiClient.js:359`. That is an operationally important lie: Stage 1 can still hit rate limits outside the queue.

9. **Medium: deploy runs dependency install/build on the production host.**  
   `.github/workflows/deploy-ec2.yml:25-30` installs dependencies and builds the SPA on EC2. That executes package lifecycle scripts on the server and requires dev/build tooling in prod. Build in CI, deploy an artifact, then restart.

10. **Medium: CI token permissions are not locked down.**  
   `.github/workflows/ci.yml` has no top-level `permissions: contents: read`. Do not leave default `GITHUB_TOKEN` scope implicit, especially on `pull_request`.

I did not run the suite; the sandbox rejected the `pnpm` process launch. The static evidence is already enough to block this diff.
